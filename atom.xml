<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>Komorebi</title>
  
  <link href="/atom.xml" rel="self"/>
  
  <link href="http://yoursite.com/"/>
  <updated>2018-08-27T01:56:27.332Z</updated>
  <id>http://yoursite.com/</id>
  
  <author>
    <name>有猫饼</name>
    
  </author>
  
  <generator uri="http://hexo.io/">Hexo</generator>
  
  <entry>
    <title>函数的基础语法</title>
    <link href="http://yoursite.com/2018/04/20/basicFunction/"/>
    <id>http://yoursite.com/2018/04/20/basicFunction/</id>
    <published>2018-04-20T07:49:19.000Z</published>
    <updated>2018-08-27T01:56:27.332Z</updated>
    
    <content type="html"><![CDATA[<p>函数是 JavaScript 中一个非常重要的概念。里面涉及到的知识点也比较多，这里我们先讲一下函数的一些基本用法。</p>
<h2 id="函数的定义"><a href="#函数的定义" class="headerlink" title="函数的定义"></a>函数的定义</h2><p>JavaScript 中创建函数的方式分为三种：</p>
<ul>
<li>声明式函数</li>
<li>函数表达式</li>
<li>构造函数</li>
</ul>
<p>其中第三种通过调用构造函数<code>new Function()</code>来创建函数的方式并不实用，所以这里我们只介绍另外两种常用的。</p>
<h3 id="声明式函数"><a href="#声明式函数" class="headerlink" title="声明式函数"></a>声明式函数</h3><p>“声明式函数”是通过关键字 function 加上函数名来创建一个函数。语法结构如下：</p>
<figure class="highlight js"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line"><span class="function"><span class="keyword">function</span> 函数名(<span class="params"></span>) </span>&#123;</div><div class="line">    <span class="comment">// 函数内部要执行代码</span></div><div class="line">&#125;</div></pre></td></tr></table></figure>
<h3 id="函数表达式"><a href="#函数表达式" class="headerlink" title="函数表达式"></a>函数表达式</h3><p>”函数表达式“是将一个没有名字的函数赋值给一个变量，变量名就作为函数名使用。语法结构如下：</p>
<figure class="highlight js"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">let</span> 函数名 = <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</div><div class="line">    <span class="comment">// 函数内部要执行代码</span></div><div class="line">&#125;</div></pre></td></tr></table></figure>
<h2 id="函数的调用"><a href="#函数的调用" class="headerlink" title="函数的调用"></a>函数的调用</h2><p>函数创建好后内部的代码并不会执行。因为函数的创建仅仅只是封装好了一个功能，要使用功能还需要对函数进行调用。</p>
<p>不管采用什么方式创建函数，调用的方法都是一样的。语法结构如下：</p>
<figure class="highlight js"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">函数名()</div></pre></td></tr></table></figure>
<p>例：</p>
<figure class="highlight js"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line"><span class="function"><span class="keyword">function</span> <span class="title">foo</span>(<span class="params"></span>) </span>&#123;</div><div class="line">    <span class="built_in">console</span>.log(<span class="string">"hello foo"</span>);</div><div class="line">&#125;</div><div class="line">foo();  <span class="comment">// 调用函数 foo</span></div></pre></td></tr></table></figure>
<h2 id="函数的返回值"><a href="#函数的返回值" class="headerlink" title="函数的返回值"></a>函数的返回值</h2><p>返回值是指可以将函数内部的一个值返回到函数外部去。</p>
<h3 id="设置返回值"><a href="#设置返回值" class="headerlink" title="设置返回值"></a>设置返回值</h3><p>在函数体内部，通过<code>return</code>关键字来设置返回值。</p>
<figure class="highlight js"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line"><span class="function"><span class="keyword">function</span> <span class="title">foo</span>(<span class="params"></span>) </span>&#123;</div><div class="line">    <span class="keyword">let</span> num = <span class="number">100</span>;</div><div class="line">    <span class="keyword">return</span> num; </div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>上例中，<code>num</code>就是函数<code>foo</code>的返回值。也就是说，我们将函数内部的数据 100 返回到了函数外部。</p>
<h3 id="接收返回值"><a href="#接收返回值" class="headerlink" title="接收返回值"></a>接收返回值</h3><p>函数内部将数据<code>return</code>出来，外部还需要先接收才能使用。而外部接收返回值的地方就是函数的调用。</p>
<figure class="highlight js"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line"><span class="built_in">console</span>.log(foo());    <span class="comment">// 100</span></div></pre></td></tr></table></figure>
<p>也就是说，使用<code>函数名()</code>其实有两个功能：一是调用函数执行函数内部代码，二是接收函数内部的返回值。</p>
<h3 id="return-特点"><a href="#return-特点" class="headerlink" title="return 特点"></a>return 特点</h3><ul>
<li><p>设置返回值并不是必须的。当函数内没有定义<code>return</code>或<code>return</code>后面没有值时，当前函数的返回值默认为<code>undefined</code>。</p>
<figure class="highlight js"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line"><span class="function"><span class="keyword">function</span> <span class="title">bar</span>(<span class="params"></span>) </span>&#123;&#125;;</div><div class="line"><span class="built_in">console</span>.log(bar());   <span class="comment">// undefined</span></div></pre></td></tr></table></figure>
</li>
<li><p>如果设置返回值，<code>return</code>语句后面只能跟一个值。如果要返回多条数据时，只能考虑将其合并成一个整体（例如：数组）。</p>
<figure class="highlight js"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div></pre></td><td class="code"><pre><div class="line"><span class="function"><span class="keyword">function</span> <span class="title">bar</span>(<span class="params"></span>) </span>&#123;</div><div class="line">    <span class="keyword">let</span> a = <span class="number">1</span>;</div><div class="line">    <span class="keyword">let</span> b = <span class="number">2</span>;</div><div class="line">    <span class="keyword">let</span> c = <span class="number">3</span>;</div><div class="line">    <span class="keyword">return</span> [a, b, c];</div><div class="line">&#125;</div></pre></td></tr></table></figure>
</li>
<li><p><code>return</code>语句一旦执行完毕，会立即结束当前函数，即函数内<code>return</code>后面的语句不会再执行。</p>
<figure class="highlight js"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div></pre></td><td class="code"><pre><div class="line"><span class="function"><span class="keyword">function</span> <span class="title">bar</span>(<span class="params"></span>) </span>&#123;</div><div class="line">    <span class="keyword">let</span> a = <span class="number">1</span>;</div><div class="line">    <span class="keyword">return</span> a;</div><div class="line">    <span class="built_in">console</span>.log(a);    <span class="comment">// 不会执行</span></div><div class="line">&#125;</div></pre></td></tr></table></figure>
</li>
</ul>
<h2 id="函数的参数"><a href="#函数的参数" class="headerlink" title="函数的参数"></a>函数的参数</h2><p>函数的参数可以看做是在函数内部使用的局部变量。</p>
<h3 id="形参和实参"><a href="#形参和实参" class="headerlink" title="形参和实参"></a>形参和实参</h3><p>一个完整的函数参数由两部分组成：</p>
<ul>
<li>形参：形式参数，指在创建函数时定义的参数（类似于局部变量名）。</li>
<li>实参：实际参数，指在调用函数时定义的参数（类似于局部变量值）。</li>
</ul>
<p>语法结构如下：</p>
<figure class="highlight js"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line"><span class="function"><span class="keyword">function</span> <span class="title">foo</span>(<span class="params">形参<span class="number">1</span>, 形参<span class="number">2</span>, ..., 形参n</span>) </span>&#123;&#125;;  <span class="comment">// 创建函数</span></div><div class="line">foo(实参<span class="number">1</span>, 实参<span class="number">2</span>, ..., 实参n);              <span class="comment">// 调用函数</span></div></pre></td></tr></table></figure>
<p>当函数调用时，会将实参传递给对应位置的形参，效果类似于变量赋值。</p>
<p>例：</p>
<figure class="highlight js"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line"><span class="function"><span class="keyword">function</span> <span class="title">bar</span>(<span class="params">num1, num2</span>) </span>&#123;</div><div class="line">    <span class="built_in">console</span>.log(num1, num2);    <span class="comment">// 100 200</span></div><div class="line">&#125;</div><div class="line">bar(<span class="number">100</span>, <span class="number">200</span>);</div></pre></td></tr></table></figure>
<p>一般情况下，形参和实参的个数是相同的，这样就能一一对应进行传值操作。但在 JavaScript 中并不强求这一点，在特殊情况下，函数的形参和实参的个数可以不相同。</p>
<p>例如：</p>
<figure class="highlight js"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line"><span class="function"><span class="keyword">function</span> <span class="title">bar</span>(<span class="params">num1</span>) </span>&#123;</div><div class="line">    <span class="built_in">console</span>.log(num1);    <span class="comment">// 1</span></div><div class="line">&#125;</div><div class="line">bar(<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>);</div></pre></td></tr></table></figure>
<p>在上例中，形参<code>num1</code>接收到了实参<code>1</code>。但是实参<code>2</code>和<code>3</code>因为没有对应的形参接收，因此函数内部是找不到这两条数据的。</p>
<h3 id="arguments-对象"><a href="#arguments-对象" class="headerlink" title="arguments 对象"></a>arguments 对象</h3><p>函数中除了可以用形参来接收实参外，还可以用函数内部的 arguments 对象来接收所有的实参。</p>
<figure class="highlight js"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line"><span class="function"><span class="keyword">function</span> <span class="title">bar</span>(<span class="params"></span>) </span>&#123;</div><div class="line">    <span class="built_in">console</span>.log(<span class="built_in">arguments</span>);   <span class="comment">//  &#123; '0': 100, '1': 200, '2': 300 &#125;</span></div><div class="line">&#125;</div><div class="line">bar(<span class="number">100</span>, <span class="number">200</span>, <span class="number">300</span>);</div></pre></td></tr></table></figure>
<p>arguments 是一个伪数组对象，它也可以通过下标来单独访问里面每一条数据。</p>
<figure class="highlight js"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div></pre></td><td class="code"><pre><div class="line"><span class="function"><span class="keyword">function</span> <span class="title">bar</span>(<span class="params"></span>) </span>&#123;</div><div class="line">    <span class="built_in">console</span>.log(<span class="built_in">arguments</span>[<span class="number">0</span>]);   <span class="comment">//  100</span></div><div class="line">    <span class="built_in">console</span>.log(<span class="built_in">arguments</span>[<span class="number">1</span>]);   <span class="comment">//  200</span></div><div class="line">    <span class="built_in">console</span>.log(<span class="built_in">arguments</span>[<span class="number">2</span>]);   <span class="comment">//  300</span></div><div class="line">&#125;</div><div class="line">bar(<span class="number">100</span>, <span class="number">200</span>, <span class="number">300</span>);</div></pre></td></tr></table></figure>
<h3 id="不定参数"><a href="#不定参数" class="headerlink" title="不定参数"></a>不定参数</h3><p>关于函数的参数，ES6 中增加了一个新的特性，叫做 rest 参数，即“不定参数”。它的作用和 arguments 对象很类似，arguments 是用来接收所有的实参，rest 是用来接收没有形参对应的所有实参。</p>
<p>语法结构：</p>
<figure class="highlight js"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line"><span class="function"><span class="keyword">function</span> <span class="title">foo</span>(<span class="params">...不定参数名</span>) </span>&#123;&#125;;</div></pre></td></tr></table></figure>
<p>例：</p>
<figure class="highlight js"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line"><span class="function"><span class="keyword">function</span> <span class="title">foo</span>(<span class="params">...nums</span>) </span>&#123;</div><div class="line">    <span class="built_in">console</span>.log(nums);    <span class="comment">// [ 100, 200, 300 ]</span></div><div class="line">&#125;</div><div class="line">foo(<span class="number">100</span>, <span class="number">200</span>, <span class="number">300</span>);</div></pre></td></tr></table></figure>
<p>在上例中，因为三个实参都没有对应的形参，所以不定参数<code>nums</code>用<strong>数组</strong>的形式接收了三个实参。</p>
<blockquote>
<p>注意：不定参数可以和普通形参同时存在，但是不定参数只能作为最后一个形参使用。</p>
</blockquote>
<figure class="highlight js"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div></pre></td><td class="code"><pre><div class="line"><span class="function"><span class="keyword">function</span> <span class="title">foo</span>(<span class="params">num1, ...nums</span>) </span>&#123;</div><div class="line">    <span class="built_in">console</span>.log(num1);    <span class="comment">// 100</span></div><div class="line">    <span class="built_in">console</span>.log(nums);    <span class="comment">// [ 200, 300 ]</span></div><div class="line">&#125;</div><div class="line">foo(<span class="number">100</span>, <span class="number">200</span>, <span class="number">300</span>);</div></pre></td></tr></table></figure>
<blockquote>
<p>当实参个数确定的情况下，我们用普通形参来接收实参;</p>
<p>当实参个数不确定的情况下，我们可以用 arguments 对象或者不定参数来接收实参。</p>
</blockquote>
<h3 id="默认参数"><a href="#默认参数" class="headerlink" title="默认参数"></a>默认参数</h3><p>默认参数，指的是函数形参的默认值。当形参没有值时，就会采用默认值。</p>
<p>我们在调用函数时，难免会存在忘记传实参的情况。一旦忘记传实参，就有可能对我们函数内部的代码造成影响。所以，一般情况下，为了避免因为忘记传参而造成的代码运行错误，我们都会给形参一个默认值。这样，当我们没有传实参的时候，形参可以采用默认值。</p>
<p>在 ES6 之前，没有专门设置形参默认值的语法。而现在在 ES6 中，它已经自己提供了一个设置默认参数的功能。语法结构如下：</p>
<figure class="highlight js"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line"><span class="function"><span class="keyword">function</span> <span class="title">foo</span>(<span class="params">形参 = 默认值</span>) </span>&#123;</div><div class="line">    <span class="comment">// ...</span></div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>例：</p>
<figure class="highlight js"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div></pre></td><td class="code"><pre><div class="line"><span class="function"><span class="keyword">function</span> <span class="title">foo</span>(<span class="params">name = <span class="string">"HanMeiMei"</span></span>) </span>&#123;</div><div class="line">    <span class="built_in">console</span>.log(<span class="string">'hello '</span> + name);    </div><div class="line">&#125;</div><div class="line">foo();              <span class="comment">// hello HanMeiMei</span></div><div class="line">foo(<span class="string">"LiZiMing"</span>);    <span class="comment">// hello LiZiMing</span></div></pre></td></tr></table></figure>
<h3 id="参数的应用场景"><a href="#参数的应用场景" class="headerlink" title="参数的应用场景"></a>参数的应用场景</h3><p>函数的参数并不是必须的，那到底什么时候需要使用参数？</p>
<p>我们知道，封装函数的目的就是为了避免书写大量重复的代码。例如，我们要书写三个人的自我介绍的功能。</p>
<figure class="highlight js"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div><div class="line">19</div><div class="line">20</div><div class="line">21</div><div class="line">22</div><div class="line">23</div></pre></td><td class="code"><pre><div class="line"><span class="function"><span class="keyword">function</span> <span class="title">introduce1</span>(<span class="params"></span>)</span>&#123;</div><div class="line">    <span class="built_in">console</span>.log(<span class="string">"你好"</span>);</div><div class="line">    <span class="built_in">console</span>.log(<span class="string">"我的名字叫韩梅梅"</span>);</div><div class="line">    <span class="built_in">console</span>.log(<span class="string">"性别女"</span>);</div><div class="line">    <span class="built_in">console</span>.log(<span class="string">"今年20岁"</span>);</div><div class="line">&#125;</div><div class="line">introduce1();</div><div class="line"></div><div class="line"><span class="function"><span class="keyword">function</span> <span class="title">introduce2</span>(<span class="params"></span>)</span>&#123;</div><div class="line">    <span class="built_in">console</span>.log(<span class="string">"你好"</span>);</div><div class="line">    <span class="built_in">console</span>.log(<span class="string">"我的名字叫马冬梅"</span>);</div><div class="line">    <span class="built_in">console</span>.log(<span class="string">"性别女"</span>);</div><div class="line">    <span class="built_in">console</span>.log(<span class="string">"今年30岁"</span>);</div><div class="line">&#125;</div><div class="line">introduce2();</div><div class="line"></div><div class="line"><span class="function"><span class="keyword">function</span> <span class="title">introduce3</span>(<span class="params"></span>)</span>&#123;</div><div class="line">    <span class="built_in">console</span>.log(<span class="string">"你好"</span>);</div><div class="line">    <span class="built_in">console</span>.log(<span class="string">"我的名字叫李子明"</span>);</div><div class="line">    <span class="built_in">console</span>.log(<span class="string">"性别男"</span>);</div><div class="line">    <span class="built_in">console</span>.log(<span class="string">"今年18岁"</span>);</div><div class="line">&#125;</div><div class="line">introduce3();</div></pre></td></tr></table></figure>
<p>在这三个方法中，存在大量重复的代码，按理说可以直接封装成一个函数。但是如果封装成一个函数，里面的姓名、年龄、性别又是不一样的，那这个时候怎么办？</p>
<figure class="highlight js"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div></pre></td><td class="code"><pre><div class="line"><span class="function"><span class="keyword">function</span> <span class="title">introduce</span>(<span class="params">name, gender, age</span>)</span>&#123;</div><div class="line">    <span class="built_in">console</span>.log(<span class="string">"你好"</span>);</div><div class="line">    <span class="built_in">console</span>.log(<span class="string">"我的名字叫"</span> + name);</div><div class="line">    <span class="built_in">console</span>.log(<span class="string">"性别"</span> + gender);</div><div class="line">    <span class="built_in">console</span>.log(<span class="string">"今年"</span> + age + <span class="string">"岁"</span>);</div><div class="line">&#125;</div><div class="line">introduce(<span class="string">"韩梅梅"</span>, <span class="string">"女"</span>, <span class="number">20</span>);</div><div class="line">introduce(<span class="string">"马冬梅"</span>, <span class="string">"女"</span>, <span class="number">30</span>);</div><div class="line">introduce(<span class="string">"李子明"</span>, <span class="string">"男"</span>, <span class="number">18</span>);</div></pre></td></tr></table></figure>
<p>函数的参数就能很好的解决这个问题。所以，当封装好的重复代码中，有小部分内容是不重复的，那么就把这小部分作为函数的参数，通过调用函数时传入不同的实参来实现这小部分代码的不重复。</p>
]]></content>
    
    <summary type="html">
    
      &lt;p&gt;函数是 JavaScript 中一个非常重要的概念。里面涉及到的知识点也比较多，这里我们先讲一下函数的一些基本用法。&lt;/p&gt;
&lt;h2 id=&quot;函数的定义&quot;&gt;&lt;a href=&quot;#函数的定义&quot; class=&quot;headerlink&quot; title=&quot;函数的定义&quot;&gt;&lt;/a&gt;函数的定义
    
    </summary>
    
      <category term="JavaScript" scheme="http://yoursite.com/categories/JavaScript/"/>
    
      <category term="函数" scheme="http://yoursite.com/categories/JavaScript/%E5%87%BD%E6%95%B0/"/>
    
    
      <category term="参数" scheme="http://yoursite.com/tags/%E5%8F%82%E6%95%B0/"/>
    
      <category term="返回值" scheme="http://yoursite.com/tags/%E8%BF%94%E5%9B%9E%E5%80%BC/"/>
    
      <category term="return" scheme="http://yoursite.com/tags/return/"/>
    
  </entry>
  
  <entry>
    <title>数组的基础语法</title>
    <link href="http://yoursite.com/2018/04/10/basicArray/"/>
    <id>http://yoursite.com/2018/04/10/basicArray/</id>
    <published>2018-04-10T07:49:19.000Z</published>
    <updated>2018-08-22T05:55:20.825Z</updated>
    
    <content type="html"><![CDATA[<p>数组对于每一门编程语言来说都是重要的数据结构之一，当然不同语言对数组的实现及处理也不尽相同。</p>
<blockquote>
<p> JavaScript 中的数组是一个可以储存多个任意类型数据的有序集合。</p>
</blockquote>
<a id="more"></a>
<p><strong>数组的特点：</strong></p>
<ol>
<li>数组的作用就是可以将多条零散的数据结合成一个整体；</li>
<li>数组本身属于“引用数据类型”，即数组是“对象”。但是数组内部储存的数据可以是任意数据类型。</li>
<li>根据需要可以对数组内部的数据进行操作（例如：增、删、查、改）</li>
</ol>
<h2 id="定义数组"><a href="#定义数组" class="headerlink" title="定义数组"></a>定义数组</h2><p>创建数组的方式分为两种：</p>
<ol>
<li><p>字面量方式</p>
<figure class="highlight js"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">[];</div></pre></td></tr></table></figure>
</li>
<li><p>构造函数方式</p>
<figure class="highlight js"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">new</span> <span class="built_in">Array</span>();</div></pre></td></tr></table></figure>
</li>
</ol>
<p>通过以上两种方式都可以创建出一个还未储存数据的空数组。其中字面量方式效率更高一些。因为方括号是使用 JSON 格式的语法，可以被 JavaScript 引擎直接解释，而 new 关键字的方式则需要调用构造函数。</p>
<p>一般情况下，为了方便后面对数组的操作使用，我们将数组创建好后都会赋值给一个变量保存起来。</p>
<figure class="highlight js"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">let</span> ary1 = [];</div><div class="line"><span class="keyword">let</span> ary2 = <span class="keyword">new</span> <span class="built_in">Array</span>();</div></pre></td></tr></table></figure>
<h3 id="数组初始化"><a href="#数组初始化" class="headerlink" title="数组初始化"></a>数组初始化</h3><p>数组初始化即在创建数组的同时就将数据添加进去。</p>
<figure class="highlight js"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">let</span> ary1 = [<span class="number">10</span>, <span class="literal">true</span>, <span class="literal">null</span>, <span class="string">'hello'</span>, <span class="literal">undefined</span>];</div><div class="line"><span class="keyword">let</span> ary2 = <span class="keyword">new</span> <span class="built_in">Array</span>(<span class="number">10</span>, <span class="literal">true</span>, <span class="literal">null</span>, <span class="string">'hello'</span>, <span class="literal">undefined</span>);</div></pre></td></tr></table></figure>
<h2 id="数组的下标"><a href="#数组的下标" class="headerlink" title="数组的下标"></a>数组的下标</h2><p>前面提到，数组里面储存的数据是有顺序的。因此，每一条数据在数组中都有一个对应的位置编号，称之为“下标/索引”。</p>
<blockquote>
<p>下标的取值从 0 开始，依次递增 1。</p>
</blockquote>
<h3 id="下标的使用"><a href="#下标的使用" class="headerlink" title="下标的使用"></a>下标的使用</h3><p>通过下标，就可以找到并操作数组中的任意数据。语法结构如下：</p>
<figure class="highlight js"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">数组名[下标]</div></pre></td></tr></table></figure>
<p><strong>查看数据</strong></p>
<figure class="highlight js"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">let</span> ary = [<span class="number">100</span>, <span class="number">200</span>, <span class="number">300</span>];</div><div class="line"><span class="built_in">console</span>.log(ary[<span class="number">0</span>]);    <span class="comment">// 100</span></div><div class="line"><span class="built_in">console</span>.log(ary[<span class="number">1</span>]);    <span class="comment">// 200</span></div><div class="line"><span class="built_in">console</span>.log(ary[<span class="number">2</span>]);    <span class="comment">// 300</span></div></pre></td></tr></table></figure>
<p><strong>修改数据</strong></p>
<figure class="highlight js"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">let</span> ary = [<span class="number">100</span>, <span class="number">200</span>, <span class="number">300</span>];</div><div class="line">ary[<span class="number">0</span>] = <span class="string">'a'</span>;</div><div class="line">ary[<span class="number">1</span>] = <span class="string">'b'</span>;</div><div class="line">ary[<span class="number">2</span>] = <span class="string">'c'</span>;</div><div class="line"><span class="built_in">console</span>.log(ary);     <span class="comment">// ['a', 'b', 'c']</span></div></pre></td></tr></table></figure>
<p><strong>新增数据</strong></p>
<figure class="highlight js"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">let</span> ary = [<span class="number">100</span>, <span class="number">200</span>, <span class="number">300</span>];</div><div class="line">ary[<span class="number">3</span>] = <span class="number">400</span>;</div><div class="line">ary[<span class="number">5</span>] = <span class="number">500</span>;</div><div class="line"><span class="built_in">console</span>.log(ary);     <span class="comment">// [100, 200, 300, 400, empty, 500];</span></div></pre></td></tr></table></figure>
<blockquote>
<p>下标 4 这个位置上没有赋值，当访问这个位置时会得到<code>undefined</code>的结果。</p>
</blockquote>
<p><strong>删除数据</strong></p>
<figure class="highlight js"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">let</span> ary = [<span class="number">100</span>, <span class="number">200</span>, <span class="number">300</span>];</div><div class="line"><span class="keyword">delete</span> ary[<span class="number">0</span>];</div><div class="line"><span class="built_in">console</span>.log(ary);     <span class="comment">// [empty, 200, 300];</span></div></pre></td></tr></table></figure>
<h2 id="数组的长度"><a href="#数组的长度" class="headerlink" title="数组的长度"></a>数组的长度</h2><p>数组的长度，即数组内保存的数据的条数。JavaScript 中为数组提供了一个<code>length</code>属性，用来设置或返回数组的长度。</p>
<p>语法结构：</p>
<figure class="highlight js"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">数组名.length</div></pre></td></tr></table></figure>
<p>例：</p>
<figure class="highlight js"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">let</span> ary = [<span class="number">100</span>, <span class="number">200</span>, <span class="number">300</span>];</div><div class="line"><span class="comment">// 获取长度</span></div><div class="line"><span class="built_in">console</span>.log(ary.length);     <span class="comment">// 3</span></div><div class="line"><span class="comment">// 设置长度</span></div><div class="line">ary.length = <span class="number">2</span>;</div><div class="line"><span class="built_in">console</span>.log(ary);            <span class="comment">// [100, 200]</span></div></pre></td></tr></table></figure>
<blockquote>
<p>小技巧：想要快速清空一个数组，就可以将数组的长度赋值为 0。</p>
</blockquote>
<h2 id="多维数组"><a href="#多维数组" class="headerlink" title="多维数组"></a>多维数组</h2><p>JavaScript 中的数组有一个特点是可以储存任意类型的数据。也就意味着，数组里面的数据也可以是数组。</p>
<figure class="highlight js"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">let</span> ary = [<span class="number">100</span>, <span class="number">200</span>, [<span class="string">'a'</span>, <span class="string">'b'</span>], <span class="number">300</span>, <span class="number">400</span>];</div></pre></td></tr></table></figure>
<p>这种数组里面嵌套数组的结构，就叫做多维数组。</p>
<p>多维数组和普通数组的操作方式一样，都是通过下标来实现：</p>
<figure class="highlight js"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">let</span> ary = [<span class="number">100</span>, <span class="number">200</span>, [<span class="string">'a'</span>, <span class="string">'b'</span>], <span class="number">300</span>, <span class="number">400</span>];</div><div class="line"><span class="comment">// 查看</span></div><div class="line"><span class="built_in">console</span>.log(ary[<span class="number">2</span>][<span class="number">0</span>]);      <span class="comment">// 'a'</span></div><div class="line"><span class="comment">// 修改</span></div><div class="line">ary[<span class="number">2</span>][<span class="number">0</span>] = <span class="string">'A'</span>;</div><div class="line"><span class="comment">// 新增</span></div><div class="line">ary[<span class="number">5</span>] = [<span class="literal">true</span>, <span class="literal">false</span>];</div><div class="line"><span class="comment">// 删除</span></div><div class="line"><span class="keyword">delete</span> ary[<span class="number">2</span>][<span class="number">0</span>];</div></pre></td></tr></table></figure>
<h2 id="数组的遍历"><a href="#数组的遍历" class="headerlink" title="数组的遍历"></a>数组的遍历</h2><p>数组的作用是可以批量保存多条数据，但是当我们需要对数据进行操作时，并不能通过直接操作数组来实现效果，还是需要将每一条数据从数组中取出来，然后再依次取对其进行操作。</p>
<blockquote>
<p> 数组的遍历就是指依次访问数组中每一项数据。</p>
</blockquote>
<h3 id="for-循环"><a href="#for-循环" class="headerlink" title="for 循环"></a>for 循环</h3><p><code>for</code>循环是遍历数组最简单的一种，也是使用频率最高的一种。</p>
<figure class="highlight js"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">let</span> ary = [<span class="number">100</span>, <span class="number">200</span>, <span class="number">300</span>, <span class="number">400</span>];</div><div class="line"><span class="comment">// i 即用来控制循环次数，也可用作数组的下标</span></div><div class="line"><span class="keyword">for</span>(<span class="keyword">let</span> i = <span class="number">0</span>; i &lt; ary.length; i++)&#123;</div><div class="line">    <span class="built_in">console</span>.log(ary[i]);</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p><strong>优化版 for 循环</strong></p>
<p>对于 for 循环的执行流程来说，<code>i &lt; ary.length</code>这个表达式会反复执行很多次，也就是说，数组的长度每次都要去重新获取。优化版 for 循环就是在普通版 for 循环基础之上，使用临时变量，将数组的长度<code>ary.length</code>缓存起来，避免重复获取数组长度。代码如下：</p>
<figure class="highlight js"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">for</span>(<span class="keyword">let</span> i = <span class="number">0</span>, len = ary.length; i &lt; len; i++)&#123;</div><div class="line">    <span class="built_in">console</span>.log(ary[i]);</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<h3 id="for-of"><a href="#for-of" class="headerlink" title="for-of"></a>for-of</h3><p>ES6 中新增了一种遍历数组的方法 for-of，相比起 for 循环来说它的代码更加简洁。</p>
<figure class="highlight js"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">let</span> ary = [<span class="number">100</span>, <span class="number">200</span>, <span class="number">300</span>, <span class="number">400</span>];</div><div class="line"><span class="comment">// 创建一个变量 value 用来接收数组中的每一个值</span></div><div class="line"><span class="keyword">for</span>(<span class="keyword">let</span> value <span class="keyword">of</span> ary)&#123;</div><div class="line">    <span class="built_in">console</span>.log(value);</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<blockquote>
<p>说明：效率上来说 for-of 还是比不上普通的 for 循环。但是 for-of 除了可以遍历数组外，还可以遍历大多数类数组对象。例如后面会讲到的 Set、Map。</p>
</blockquote>
]]></content>
    
    <summary type="html">
    
      &lt;p&gt;数组对于每一门编程语言来说都是重要的数据结构之一，当然不同语言对数组的实现及处理也不尽相同。&lt;/p&gt;
&lt;blockquote&gt;
&lt;p&gt; JavaScript 中的数组是一个可以储存多个任意类型数据的有序集合。&lt;/p&gt;
&lt;/blockquote&gt;
    
    </summary>
    
      <category term="JavaScript" scheme="http://yoursite.com/categories/JavaScript/"/>
    
      <category term="数组" scheme="http://yoursite.com/categories/JavaScript/%E6%95%B0%E7%BB%84/"/>
    
    
      <category term="for-of" scheme="http://yoursite.com/tags/for-of/"/>
    
  </entry>
  
  <entry>
    <title>循环语句</title>
    <link href="http://yoursite.com/2018/03/17/loopStatements/"/>
    <id>http://yoursite.com/2018/03/17/loopStatements/</id>
    <published>2018-03-17T07:49:19.000Z</published>
    <updated>2018-08-21T02:35:22.388Z</updated>
    
    <content type="html"><![CDATA[<p>前面提到流程控制语句主要分为顺序执行、选择执行和循环执行。</p>
<p>循环语句，就是用来实现”循环执行“的代码执行顺序。</p>
<a id="more"></a>
<blockquote>
<p>循环语句原理：通过判断某些条件是否成立，来决定某一部分代码反复执行的次数。</p>
</blockquote>
<p>循环语句主要分为三种形式：</p>
<ol>
<li>while 循环</li>
<li>do while 循环</li>
<li>for 循环</li>
</ol>
<h2 id="while-循环"><a href="#while-循环" class="headerlink" title="while 循环"></a>while 循环</h2><p>while 循环语句，由一个<code>while</code>构成，语法结构如下：</p>
<figure class="highlight js"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">while</span>(条件)&#123;</div><div class="line">    代码块</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>代码执行顺序：</p>
<ol>
<li>判断小括号中条件是否成立；</li>
<li>条件成立，执行代码块所有代码；</li>
<li>重复第 1、2 步，直到小括号中条件不成立，循环结束。</li>
</ol>
<figure class="highlight js"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">let</span> num = <span class="number">3</span>;</div><div class="line"><span class="keyword">while</span>(num &gt; <span class="number">1</span>)&#123;</div><div class="line">    <span class="built_in">console</span>.log(num);</div><div class="line">    num--;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>运行结果：</p>
<figure class="highlight js"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line"><span class="number">3</span></div><div class="line"><span class="number">2</span></div></pre></td></tr></table></figure>
<p>代码执行顺序分析：</p>
<ol>
<li>执行第一行代码，创建一个变量<code>num</code>值为<code>3</code>；</li>
<li>执行第二行代码，判断条件<code>num &gt; 1</code>是否成立：<code>3 &gt; 1</code>成立；</li>
<li>进入<code>while</code>内部，执行第三、四行代码，输出<code>3</code>后<code>num</code>减 1 变成 2；</li>
<li>执行第二行代码，继续判断条件<code>num &gt; 1</code>是否成立：<code>2 &gt; 1</code>成立；</li>
<li>进入<code>while</code>内部，执行第三、四行代码，输出<code>2</code>后<code>num</code>减 1 变成 1；</li>
<li>执行第二行代码，继续判断条件<code>num &gt; 1</code>是否成立：<code>1 &gt; 1</code>不成立；</li>
<li>循环结束；</li>
</ol>
<h2 id="do-while-循环"><a href="#do-while-循环" class="headerlink" title="do while 循环"></a>do while 循环</h2><p>do while 循环语句，由一个<code>do</code>和一个<code>while</code>构成，语法结构如下：</p>
<figure class="highlight js"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">do</span>&#123;</div><div class="line">    代码块</div><div class="line">&#125;<span class="keyword">while</span>(条件)</div></pre></td></tr></table></figure>
<p>代码执行顺序：</p>
<ol>
<li>进入<code>do</code>，执行代码块中的所有代码；</li>
<li>执行第三行代码，判断条件是否成立；</li>
<li>条件成立，继续执行代码块所有代码；</li>
<li>重复第 2、3 步，直到小括号中条件不成立，循环结束。</li>
</ol>
<figure class="highlight js"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">let</span> num = <span class="number">3</span>;</div><div class="line"><span class="keyword">do</span>&#123;</div><div class="line">    <span class="built_in">console</span>.log(num);</div><div class="line">    num--;</div><div class="line">&#125;<span class="keyword">while</span>(num &gt; <span class="number">1</span>);</div></pre></td></tr></table></figure>
<p>运行结果：</p>
<figure class="highlight js"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line"><span class="number">3</span></div><div class="line"><span class="number">2</span></div></pre></td></tr></table></figure>
<p>代码执行顺序分析：</p>
<ol>
<li>执行第一行代码，创建一个变量<code>num</code>值为<code>3</code>；</li>
<li>进入<code>do</code>内部，执行第三、四行代码，输出<code>3</code>后<code>num</code>减 1 变成 2；</li>
<li>执行第五行代码，判断条件<code>num &gt; 1</code>是否成立：<code>2 &gt; 1</code>成立；</li>
<li>进入<code>do</code>内部，执行第三、四行代码，输出<code>2</code>后<code>num</code>减 1 变成 1；</li>
<li>执行第五行代码，继续判断条件<code>num &gt; 1</code>是否成立：<code>1 &gt; 1</code>不成立；</li>
<li>循环结束；</li>
</ol>
<h2 id="for-循环"><a href="#for-循环" class="headerlink" title="for 循环"></a>for 循环</h2><p><code>for</code>循环语句，由一个<code>for</code>构成，语法结构如下：</p>
<figure class="highlight js"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">for</span>(表达式一; 表达式二; 表达式三)&#123;</div><div class="line">    代码块四</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p><code>for</code>循环语句中包含了四个部分：</p>
<ul>
<li>表达式一：<strong>初始化表达式</strong>，初始化一个计数器（用于控制循环次数），在循环开始前记录初始状态。</li>
<li>表达式二：<strong>条件判断表达式</strong>，判断给定的状态是否为 true。如果条件为 true，则执行循环体，否则跳出循环。</li>
<li>表达式三：<strong>改变循环条件</strong>，修改计数器的值。</li>
<li>代码块四：<strong>循环体</strong>，需要循环执行的代码块。</li>
</ul>
<p>代码执行顺序：</p>
<ol>
<li>执行表达式一（初始化表达式）；</li>
<li>执行表达式二（条件判断表达式），判断条件是否成立；</li>
<li>表达式二条件成立，执行代码块四（循环体）；</li>
<li>执行表达式三（改变循环条件）；</li>
<li>重复第 2、3、4 步，直到表达式二条件不成立，结束整个<code>for</code>循环；</li>
</ol>
<blockquote>
<p>简单来说，<code>for</code>循环的执行顺序就是“一 二四三 二四三 二四三 … … 二”。</p>
</blockquote>
<figure class="highlight js"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">for</span>(<span class="keyword">let</span> i = <span class="number">0</span>; i &lt; <span class="number">2</span>; i++)&#123;</div><div class="line">    <span class="built_in">console</span>.log(i);</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>运行结果：</p>
<figure class="highlight js"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line"><span class="number">0</span></div><div class="line"><span class="number">1</span></div></pre></td></tr></table></figure>
<p>代码执行顺序分析：</p>
<ol>
<li>执行第一行代码中的<strong>表达式一</strong><code>let i = 0</code>；</li>
<li>判断第一行代码中的<strong>表达式二</strong><code>i &lt; 2</code>是否成立：<code>0 &lt; 2</code>成立；</li>
<li>执行第二行的<strong>代码块四</strong>，输出<code>i</code>的值<code>0</code>；</li>
<li>执行第一行代码中的<strong>表达式三</strong><code>i++</code>，将<code>i</code>的值加 1 变成 1；</li>
<li>判断第一行代码中的<strong>表达式二</strong><code>i &lt; 2</code>是否成立：<code>1 &lt; 2</code>成立；</li>
<li>执行第二行的<strong>代码块四</strong>，输出<code>i</code>的值<code>1</code>；</li>
<li>执行第一行代码中的<strong>表达式三</strong><code>i++</code>，将<code>i</code>的值加 1 变成 2；</li>
<li>判断第一行代码中的<strong>表达式二</strong><code>i &lt; 2</code>是否成立：<code>2 &lt; 2</code>不成立；</li>
<li><code>for</code>循环结束；</li>
</ol>
<h2 id="打断循环"><a href="#打断循环" class="headerlink" title="打断循环"></a>打断循环</h2><p>正常情况下，循环语句是在循环条件判断不成立后结束。除了通过这种方式结束循环外，JavaScript 还提供了两种语句用来打断循环。</p>
<ul>
<li>break</li>
<li>continue</li>
</ul>
<h3 id="break"><a href="#break" class="headerlink" title="break"></a>break</h3><p><code>break</code>在<code>switch</code>语句中用于跳出<code>switch</code>语句，即结束整个<code>switch</code>。同理，在循环语句中使用<code>break</code>，也可以实现立即结束整个循环的效果。</p>
<figure class="highlight js"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">let</span> count = <span class="number">0</span>;</div><div class="line"><span class="keyword">while</span>(count &lt; <span class="number">3</span>)&#123;</div><div class="line">    count++;</div><div class="line">    <span class="keyword">if</span>(count == <span class="number">2</span>)&#123;</div><div class="line">        <span class="keyword">break</span>;</div><div class="line">    &#125;</div><div class="line">    <span class="built_in">console</span>.log(count);</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>运行结果：</p>
<figure class="highlight js"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line"><span class="number">1</span></div></pre></td></tr></table></figure>
<p>代码执行顺序分析：</p>
<ol>
<li>执行第一行代码，创建一个变量<code>count</code>值为<code>0</code>；</li>
<li>执行第二行代码，判断循环条件<code>count &lt; 3</code>成立，进入<code>while</code>；</li>
<li>执行第三行代码，<code>count</code>加 1 变为 1；</li>
<li>执行第四行代码，判断条件<code>count == 2</code>是否成立：<code>1 == 2</code>不成立；</li>
<li>跳过<code>if</code>，执行第七行代码，输出<code>1</code>；</li>
<li>执行第二行代码，判断循环条件<code>count &lt; 3</code>成立，进入<code>while</code>；</li>
<li>执行第三行代码，<code>count</code>加 1 变为 2；</li>
<li>执行第四行代码，判断条件<code>count == 2</code>是否成立：<code>2 == 2</code>成立；</li>
<li>进入<code>if</code>，执行第五行代码<code>break</code>；</li>
<li>循环结束；</li>
</ol>
<blockquote>
<p>一旦<code>break</code>语句执行，会立即结束离<code>break</code>最近的一个循环语句。</p>
</blockquote>
<h3 id="continue"><a href="#continue" class="headerlink" title="continue"></a>continue</h3><p><code>continue</code>也可以用来结束循环，但是它只能结束当前这一次循环，会立即进行下一次的循环条件判断。</p>
<figure class="highlight js"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">let</span> count = <span class="number">0</span>;</div><div class="line"><span class="keyword">while</span>(count &lt; <span class="number">3</span>)&#123;</div><div class="line">    count++;</div><div class="line">    <span class="keyword">if</span>(count == <span class="number">2</span>)&#123;</div><div class="line">        <span class="keyword">continue</span>;</div><div class="line">    &#125;</div><div class="line">    <span class="built_in">console</span>.log(count);</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>运行结果：</p>
<figure class="highlight js"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line"><span class="number">1</span></div><div class="line"><span class="number">3</span></div></pre></td></tr></table></figure>
<p>代码执行顺序分析：</p>
<ol>
<li>执行第一行代码，创建一个变量<code>count</code>值为<code>0</code>；</li>
<li>执行第二行代码，判断循环条件<code>count &lt; 3</code>成立，进入<code>while</code>；</li>
<li>执行第三行代码，<code>count</code>加 1 变为 1；</li>
<li>执行第四行代码，判断条件<code>count == 2</code>是否成立：<code>1 == 2</code>不成立；</li>
<li>跳过<code>if</code>，执行第七行代码，输出<code>1</code>；</li>
<li>执行第二行代码，判断循环条件<code>count &lt; 3</code>成立，进入<code>while</code>；</li>
<li>执行第三行代码，<code>count</code>加 1 变为 2；</li>
<li>执行第四行代码，判断条件<code>count == 2</code>是否成立：<code>2 == 2</code>成立；</li>
<li>进入<code>if</code>，执行第五行代码<code>continue</code>；</li>
<li>执行第二行代码，判断循环条件<code>count &lt; 3</code>成立，进入<code>while</code>；</li>
<li>执行第三行代码，<code>count</code>加 1 变为 3；</li>
<li>执行第四行代码，判断条件<code>count == 2</code>是否成立：<code>3 == 2</code>不成立；</li>
<li>跳过<code>if</code>，执行第七行代码，输出<code>3</code>；</li>
<li>执行第二行代码，判断循环条件<code>count &lt; 3</code>不成立；</li>
<li>循环结束；</li>
</ol>
<blockquote>
<p> 一旦<code>continue</code>语句执行，会立即结束离<code>continue</code>最近的一个循环语句的当前这一次循环。</p>
</blockquote>
]]></content>
    
    <summary type="html">
    
      &lt;p&gt;前面提到流程控制语句主要分为顺序执行、选择执行和循环执行。&lt;/p&gt;
&lt;p&gt;循环语句，就是用来实现”循环执行“的代码执行顺序。&lt;/p&gt;
    
    </summary>
    
      <category term="JavaScript" scheme="http://yoursite.com/categories/JavaScript/"/>
    
      <category term="流程控制语句" scheme="http://yoursite.com/categories/JavaScript/%E6%B5%81%E7%A8%8B%E6%8E%A7%E5%88%B6%E8%AF%AD%E5%8F%A5/"/>
    
    
      <category term="while" scheme="http://yoursite.com/tags/while/"/>
    
      <category term="do while" scheme="http://yoursite.com/tags/do-while/"/>
    
      <category term="for" scheme="http://yoursite.com/tags/for/"/>
    
  </entry>
  
  <entry>
    <title>条件语句</title>
    <link href="http://yoursite.com/2018/03/16/conditionalStatements/"/>
    <id>http://yoursite.com/2018/03/16/conditionalStatements/</id>
    <published>2018-03-16T07:49:19.000Z</published>
    <updated>2018-08-21T02:36:04.179Z</updated>
    
    <content type="html"><![CDATA[<p>所谓流程控制，就是指对于流程的执行顺序进行控制。默认情况下，JavaScript 引擎会按照代码的编写顺序，从上往下依次执行代码语句。这种情况一般称之为“顺序执行”。</p>
<p>但不是所有的程序都只靠顺序执行就能够完成的，所以除了顺序执行之外，还需要掌握其他的一些代码执行流程。</p>
<a id="more"></a>
<p>JavaScript 中代码的执行顺序大致可以分为三种：</p>
<p><img src="https://ws4.sinaimg.cn/large/006tNbRwgy1fucsjlrgz8j31gc0kstad.jpg" alt=""></p>
<p>而条件语句，就是用来实现”选择执行“的代码执行顺序。</p>
<blockquote>
<p>条件语句原理：通过判断某些条件是否成立，来决定执行哪一部分代码或者不执行哪一部分代码。</p>
</blockquote>
<p>条件语句主要分为四种形式：</p>
<ol>
<li>单分支语句</li>
<li>双分支语句</li>
<li>多分支语句</li>
<li>多分支等比语句</li>
</ol>
<h2 id="单分支语句"><a href="#单分支语句" class="headerlink" title="单分支语句"></a>单分支语句</h2><p>单分支条件语句，由一个<code>if</code>构成，语法结构如下：</p>
<figure class="highlight js"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">if</span>(条件)&#123;</div><div class="line">    代码块</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>代码执行顺序：</p>
<ol>
<li>判断小括号中的条件是否成立；</li>
<li>条件结果为<code>true</code>，执行大括号中的代码块；</li>
<li>条件结果为<code>false</code>，跳过大括号中的代码块直接执行大括号后面的代码；</li>
</ol>
<figure class="highlight js"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">let</span> num = <span class="number">10</span>;</div><div class="line"><span class="keyword">if</span>(num &gt; <span class="number">0</span>)&#123;</div><div class="line">    <span class="built_in">console</span>.log(<span class="string">"进入 if 语句"</span>);   </div><div class="line">&#125;</div><div class="line"><span class="built_in">console</span>.log(<span class="string">"if 后面的语句"</span>);</div></pre></td></tr></table></figure>
<p>运行结果：</p>
<figure class="highlight js"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line"><span class="string">"进入 if 语句"</span></div><div class="line"><span class="string">"if 后面的语句"</span></div></pre></td></tr></table></figure>
<p>代码执行顺序分析：</p>
<ol>
<li>执行第一行，创建一个变量<code>num</code>值为<code>10</code>；</li>
<li>执行第二行，判断<code>if</code>条件<code>num &gt; 0</code>是否成立：<code>10 &gt; 0</code>成立；</li>
<li>进入<code>if</code>内部，执行第三行代码，输出<code>&quot;进入 if 语句&quot;</code>；</li>
<li>执行第五行，输出<code>&quot;if 后面的语句&quot;</code>；</li>
</ol>
<h2 id="双分支语句"><a href="#双分支语句" class="headerlink" title="双分支语句"></a>双分支语句</h2><p>双分支条件语句，由一个<code>if</code>和一个<code>else</code>构成，语法结构如下：</p>
<figure class="highlight js"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">if</span>(条件)&#123;</div><div class="line">    代码块一</div><div class="line">&#125; <span class="keyword">else</span> &#123;</div><div class="line">    代码块二</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>代码执行顺序：</p>
<ol>
<li>判断小括号中的条件是否成立；</li>
<li>条件结果为<code>true</code>，执行代码块一，跳过代码块二；</li>
<li>条件结果为<code>false</code>，跳过代码块一，执行代码块二；</li>
</ol>
<figure class="highlight js"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">let</span> num = <span class="number">10</span>;</div><div class="line"><span class="keyword">if</span>(num &gt; <span class="number">20</span>)&#123;</div><div class="line">    <span class="built_in">console</span>.log(<span class="string">"num &gt; 20 条件成立"</span>);   </div><div class="line">&#125; <span class="keyword">else</span> &#123;</div><div class="line">    <span class="built_in">console</span>.log(<span class="string">"num &gt; 20 条件不成立"</span>);</div><div class="line">&#125;</div><div class="line"><span class="built_in">console</span>.log(<span class="string">"if else 后面的语句"</span>);</div></pre></td></tr></table></figure>
<p>运行结果：</p>
<figure class="highlight js"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line"><span class="string">"num &gt; 20 条件不成立"</span></div><div class="line"><span class="string">"if else 后面的语句"</span></div></pre></td></tr></table></figure>
<p>代码执行顺序分析：</p>
<ol>
<li>执行第一行，创建一个变量<code>num</code>值为<code>10</code>；</li>
<li>执行第二行，判断<code>if</code>条件<code>num &gt; 20</code>是否成立：<code>10 &gt; 20</code>不成立；</li>
<li>跳过第三行代码；</li>
<li>进入<code>else</code>，执行第五行代码，输出<code>&quot;num &gt; 20 条件不成立&quot;</code>；</li>
<li>执行第七行代码，输出<code>&quot;if else 后面的语句&quot;</code>；</li>
</ol>
<h2 id="多分支语句"><a href="#多分支语句" class="headerlink" title="多分支语句"></a>多分支语句</h2><p>多分支条件语句，即可以设置多个判断条件的语句。由一个<code>if</code>和多个<code>else if</code>组成，语法结构如下：</p>
<figure class="highlight xquery"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">if</span>(条件一)&#123;</div><div class="line">    代码块一</div><div class="line">&#125; <span class="keyword">else</span> <span class="keyword">if</span>(条件二)&#123;</div><div class="line">    代码块</div><div class="line">&#125; <span class="keyword">else</span> <span class="keyword">if</span>(条件n)&#123;</div><div class="line">    代码块n</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>代码执行顺序：</p>
<ol>
<li>判断条件一是否成立；</li>
<li>若条件一成立，执行代码块一；</li>
<li>若条件一不成立，继续判断下一个条件是否成立；</li>
<li>一旦成功进入某一个条件后，后面的判断都不再执行；</li>
</ol>
<figure class="highlight js"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">let</span> score = <span class="number">85</span>;</div><div class="line"><span class="keyword">if</span>(score &gt;= <span class="number">90</span>)&#123;</div><div class="line">    <span class="built_in">console</span>.log(<span class="string">"优秀"</span>);</div><div class="line">&#125; <span class="keyword">else</span> <span class="keyword">if</span>(score &gt;= <span class="number">70</span>)&#123;</div><div class="line">    consle.log(<span class="string">"良好"</span>);</div><div class="line">&#125; <span class="keyword">else</span> <span class="keyword">if</span>(score &gt;= <span class="number">60</span>)&#123;</div><div class="line">    <span class="built_in">console</span>.log(<span class="string">"一般"</span>);</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>代码执行顺序分析：</p>
<ol>
<li>执行第一行代码，创建一个变量<code>score</code>保存一个成绩数据<code>85</code>;</li>
<li>执行第二行代码，判断<code>if</code>条件<code>score &gt;= 90</code>是否成立：<code>85 &gt;= 90</code>不成立；</li>
<li>跳过第三行代码；</li>
<li>执行第四行代码，判断<code>else if</code>条件<code>socre &gt;= 70</code>是否成立：<code>85 &gt;= 70</code>成立；</li>
<li>进入<code>else if</code>，执行第五行代码，输出<code>&quot;良好&quot;</code>;</li>
<li>整个多分支语句结束，继续执行后面的代码；</li>
</ol>
<p><strong>附：</strong>多分支条件语句还可以结合<code>else</code>一起使用。语法结构如下：</p>
<figure class="highlight js"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">if</span>(条件一)&#123;</div><div class="line">    代码块一</div><div class="line">&#125; <span class="keyword">else</span> <span class="keyword">if</span>(条件二)&#123;</div><div class="line">    代码块</div><div class="line">&#125; <span class="keyword">else</span> <span class="keyword">if</span>(条件n)&#123;</div><div class="line">    代码块n</div><div class="line">&#125; <span class="keyword">else</span> &#123;</div><div class="line">    代码块n+<span class="number">1</span></div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>当前面所有条件都不成立时，就会执行<code>else</code>的代码。</p>
<figure class="highlight js"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">let</span> score = <span class="number">55</span>;</div><div class="line"><span class="keyword">if</span>(score &gt;= <span class="number">90</span>)&#123;</div><div class="line">    <span class="built_in">console</span>.log(<span class="string">"优秀"</span>);</div><div class="line">&#125; <span class="keyword">else</span> <span class="keyword">if</span>(score &gt;= <span class="number">70</span>)&#123;</div><div class="line">    consle.log(<span class="string">"良好"</span>);</div><div class="line">&#125; <span class="keyword">else</span> <span class="keyword">if</span>(score &gt;= <span class="number">60</span>)&#123;</div><div class="line">    <span class="built_in">console</span>.log(<span class="string">"一般"</span>);</div><div class="line">&#125; <span class="keyword">else</span> &#123;</div><div class="line">    <span class="built_in">console</span>.log(<span class="string">"不及格"</span>);</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>依次判断前面每一个条件，都不成立，最后进入<code>else</code>，输出<code>&quot;不及格&quot;</code>。</p>
<h2 id="多分支等比语句"><a href="#多分支等比语句" class="headerlink" title="多分支等比语句"></a>多分支等比语句</h2><p>多分支等比语句，即可以设置多个判断条件（判断是否相等）的语句。一个完整的多分支等比语句由一个<code>switch</code>、多个<code>case</code>、多个<code>break</code>（可选）和一个<code>default</code>（可选）组成，语法结构如下：</p>
<figure class="highlight js"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">switch</span>(匹配条件)&#123;</div><div class="line">    <span class="keyword">case</span> 匹配值<span class="number">1</span>:</div><div class="line">        代码块一</div><div class="line">        <span class="keyword">break</span>;</div><div class="line">    <span class="keyword">case</span> 匹配值<span class="number">2</span>:</div><div class="line">        代码块二</div><div class="line">        <span class="keyword">break</span>;</div><div class="line">    <span class="keyword">case</span> 匹配值n:</div><div class="line">        代码块n</div><div class="line">        <span class="keyword">break</span>;</div><div class="line">    <span class="keyword">default</span>:</div><div class="line">        代码块n+<span class="number">1</span></div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>代码执行顺序：</p>
<ol>
<li>判断<code>匹配条件 === 匹配值1</code>是否成立；</li>
<li>若成立，则执行代码块一；</li>
<li>若不成立，则继续判断<code>匹配条件 === 匹配值2</code>是否成立；</li>
<li>如果所有<code>case</code>值都不匹配，则进入<code>default</code>，执行代码块n+1；</li>
</ol>
<figure class="highlight js"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">let</span> floor = <span class="string">'2F'</span>;</div><div class="line"><span class="keyword">switch</span>(floor)&#123;</div><div class="line">    <span class="keyword">case</span> <span class="string">'1F'</span>:</div><div class="line">        <span class="built_in">console</span>.log(<span class="string">'一楼到了'</span>);</div><div class="line">        <span class="keyword">break</span>;</div><div class="line">    <span class="keyword">case</span> <span class="string">'2F'</span>:</div><div class="line">        <span class="built_in">console</span>.log(<span class="string">'二楼到了'</span>);</div><div class="line">        <span class="keyword">break</span>;</div><div class="line">    <span class="keyword">case</span> <span class="string">'3F'</span>:</div><div class="line">        <span class="built_in">console</span>.log(<span class="string">'三楼到了'</span>);</div><div class="line">        <span class="keyword">break</span>;</div><div class="line">    <span class="keyword">default</span>:</div><div class="line">        <span class="built_in">console</span>.log(<span class="string">'楼层输入有误，请重新输入。'</span>);</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>代码执行顺序分析：</p>
<ol>
<li>执行第一行代码，创建一个变量<code>floor</code>值为<code>&#39;2F&#39;</code>;</li>
<li>进入<code>switch</code>，判断小括号中的<code>floor</code>是否全等于第一个<code>case</code>后面的匹配值<code>&#39;1F&#39;</code>：<code>floor === &#39;1F&#39;</code>不成立；</li>
<li>跳过第四、五行代码；</li>
<li>判断<code>floor</code>是否全等于第二个<code>case</code>：<code>floor === &#39;2F&#39;</code>成立；</li>
<li>执行第七行，输出<code>&#39;二楼到了&#39;</code>;</li>
<li>执行第八行<code>break</code>，跳出整个<code>switch</code>语句；</li>
</ol>
<blockquote>
<p>说明：<code>switch</code>语句有一个特点，一旦进入某一个<code>case</code>后，如果不通过<code>break</code>语句跳出，后面所有<code>case</code>的代码块都会依次执行。</p>
</blockquote>
]]></content>
    
    <summary type="html">
    
      &lt;p&gt;所谓流程控制，就是指对于流程的执行顺序进行控制。默认情况下，JavaScript 引擎会按照代码的编写顺序，从上往下依次执行代码语句。这种情况一般称之为“顺序执行”。&lt;/p&gt;
&lt;p&gt;但不是所有的程序都只靠顺序执行就能够完成的，所以除了顺序执行之外，还需要掌握其他的一些代码执行流程。&lt;/p&gt;
    
    </summary>
    
      <category term="JavaScript" scheme="http://yoursite.com/categories/JavaScript/"/>
    
      <category term="流程控制语句" scheme="http://yoursite.com/categories/JavaScript/%E6%B5%81%E7%A8%8B%E6%8E%A7%E5%88%B6%E8%AF%AD%E5%8F%A5/"/>
    
    
      <category term="if" scheme="http://yoursite.com/tags/if/"/>
    
      <category term="else" scheme="http://yoursite.com/tags/else/"/>
    
      <category term="switch" scheme="http://yoursite.com/tags/switch/"/>
    
  </entry>
  
  <entry>
    <title>运算符</title>
    <link href="http://yoursite.com/2018/03/02/operator/"/>
    <id>http://yoursite.com/2018/03/02/operator/</id>
    <published>2018-03-02T02:00:00.000Z</published>
    <updated>2018-08-21T02:29:30.198Z</updated>
    
    <content type="html"><![CDATA[<p>JavaScript 中提供了大量的运算符用于数据的操作。</p>
<a id="more"></a>
<h2 id="字符串拼接运算符"><a href="#字符串拼接运算符" class="headerlink" title="字符串拼接运算符"></a>字符串拼接运算符</h2><p>字符串拼接运算符用于 string（字符串）类型数据的合并。</p>
<table>
<thead>
<tr>
<th>运算符</th>
<th>说明</th>
<th>举例</th>
</tr>
</thead>
<tbody>
<tr>
<td>+</td>
<td>将多个字符串拼接成一个整体</td>
<td>“hello” + “world”</td>
</tr>
</tbody>
</table>
<p><code>+</code>是 JavaScript 中比较特殊的一个运算符。它除了具备数学中的加法运算功能外，当操作 string（字符串）类型的数据时，它还具备拼接数据的功能。</p>
<figure class="highlight js"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">let</span> str1 = <span class="string">'hello'</span>;</div><div class="line"><span class="keyword">let</span> str2 = <span class="string">'HanMeiMei'</span>;</div><div class="line"><span class="keyword">let</span> str = str1 + str2;</div><div class="line"><span class="built_in">console</span>.log(str);   <span class="comment">// helloHanMeiMei</span></div></pre></td></tr></table></figure>
<blockquote>
<p>注意：只要操作的数据中存在一个 string 类型，那么<code>+</code>就作为字符串拼接运算符使用。</p>
</blockquote>
<figure class="highlight js"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">let</span> num = <span class="number">10</span>;</div><div class="line"><span class="keyword">let</span> str = <span class="string">'20'</span>;</div><div class="line"><span class="keyword">let</span> sum = num + str;</div><div class="line"><span class="built_in">console</span>.log(sum);   <span class="comment">// 1020</span></div></pre></td></tr></table></figure>
<h2 id="算术运算符"><a href="#算术运算符" class="headerlink" title="算术运算符"></a>算术运算符</h2><p>算术运算符用于 number（数值）类型数据的数学计算。</p>
<p>给定<code>a = 4, b = 2</code>条件，所有算术运算符描述如下：</p>
<table>
<thead>
<tr>
<th>运算符</th>
<th>说明</th>
<th>举例</th>
</tr>
</thead>
<tbody>
<tr>
<td>+</td>
<td>多个运算数相加</td>
<td>a + b = 6</td>
</tr>
<tr>
<td>-</td>
<td>多个运算数相减</td>
<td>a - b = 2</td>
</tr>
<tr>
<td>*</td>
<td>多个运算数相乘</td>
<td>a * b = 8</td>
</tr>
<tr>
<td>/</td>
<td>多个运算数相除</td>
<td>a / b = 2</td>
</tr>
<tr>
<td>%</td>
<td>计算多个运算数整除后的余数</td>
<td>a % b = 0</td>
</tr>
<tr>
<td>**</td>
<td>求幂，计算当前运算数的 n 次方</td>
<td>a ** b = 16</td>
</tr>
<tr>
<td>++</td>
<td>自增，当前运算数的值加 1</td>
<td>a++</td>
</tr>
<tr>
<td>–</td>
<td>自减，当前运算数的值减 1</td>
<td>–b</td>
</tr>
</tbody>
</table>
<p>自增（自减）运算符还分了两种情况：</p>
<ul>
<li><p><strong>前置型</strong>：运算符在变量前。例：<code>++a</code>。</p>
<blockquote>
<p>数据先自增（自减），再使用数据。</p>
</blockquote>
</li>
<li><p><strong>后置型</strong>：运算符在变量后。例：<code>a++</code>。</p>
<blockquote>
<p>先使用数据，数据再自增（自减）。</p>
</blockquote>
</li>
</ul>
<figure class="highlight js"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">let</span> a = <span class="number">10</span>;</div><div class="line"><span class="built_in">console</span>.log(a++);  <span class="comment">// 10</span></div><div class="line"><span class="built_in">console</span>.log(a);    <span class="comment">// 11</span></div><div class="line"></div><div class="line"><span class="keyword">let</span> b = <span class="number">10</span>;</div><div class="line"><span class="built_in">console</span>.log(++b);  <span class="comment">// 11</span></div><div class="line"><span class="built_in">console</span>.log(b);    <span class="comment">// 11</span></div></pre></td></tr></table></figure>
<h2 id="比较运算符"><a href="#比较运算符" class="headerlink" title="比较运算符"></a>比较运算符</h2><p>比较运算符用于 number（数值）类型和 string（字符串）类型数据的比较，比较结果为布尔值（<code>true</code>/<code>false</code>）。</p>
<p>给定<code>a = 4, b = 2</code>条件，所有比较运算符描述如下：</p>
<table>
<thead>
<tr>
<th>运算符</th>
<th>说明</th>
<th>举例</th>
</tr>
</thead>
<tbody>
<tr>
<td>&gt;</td>
<td>判断左边是否大于右边</td>
<td>a &gt; b</td>
</tr>
<tr>
<td>&lt;</td>
<td>判断左边是否小于右边</td>
<td>a &lt; b</td>
</tr>
<tr>
<td>&gt;=</td>
<td>判断左边是否大于等于右边</td>
<td>a &gt;= b</td>
</tr>
<tr>
<td>&lt;=</td>
<td>判断左边是否小于右边</td>
<td>a &lt;= b</td>
</tr>
<tr>
<td>==</td>
<td>判断左右两边的数据值是否相等</td>
<td>a == b</td>
</tr>
<tr>
<td>===</td>
<td>判断左右两边的数据值和数据类型是否都相等</td>
<td>a == b</td>
</tr>
<tr>
<td>!=</td>
<td>判断左右两边的数据值是否不相等</td>
<td>a != b</td>
</tr>
<tr>
<td>!==</td>
<td>判断左右两边的数据值或数据类型是否不相等</td>
<td>a !== b</td>
</tr>
</tbody>
</table>
<figure class="highlight js"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">let</span> a = <span class="number">10</span>;</div><div class="line"><span class="keyword">let</span> b = <span class="string">'10'</span>;</div><div class="line"><span class="built_in">console</span>.log(a == b);   <span class="comment">// true  </span></div><div class="line"><span class="built_in">console</span>.log(a === b);  <span class="comment">// false </span></div><div class="line"><span class="built_in">console</span>.log(a != b);   <span class="comment">// false</span></div><div class="line"><span class="built_in">console</span>.log(a !== b);  <span class="comment">// true</span></div></pre></td></tr></table></figure>
<h2 id="赋值运算符"><a href="#赋值运算符" class="headerlink" title="赋值运算符"></a>赋值运算符</h2><p>赋值运算符用于数据的赋值。</p>
<p>给定<code>a = 4, b = 2</code>条件，所有赋值运算符描述如下：</p>
<table>
<thead>
<tr>
<th>运算符</th>
<th>说明</th>
<th>举例</th>
</tr>
</thead>
<tbody>
<tr>
<td>=</td>
<td>将右边的数据赋值给左边</td>
<td>a = b</td>
</tr>
<tr>
<td>+=</td>
<td>将左右两边相加的结果赋值给左边</td>
<td>a += b</td>
</tr>
<tr>
<td>-=</td>
<td>将左右两边相减的结果赋值给左边</td>
<td>a -= b</td>
</tr>
<tr>
<td>*=</td>
<td>将左右两边相乘的结果赋值给左边</td>
<td>a *= b</td>
</tr>
<tr>
<td>/=</td>
<td>将左右两边相除的结果赋值给左边</td>
<td>a /= b</td>
</tr>
<tr>
<td>%=</td>
<td>将左右两边相除的余数赋值给左边</td>
<td>a %= b</td>
</tr>
</tbody>
</table>
<blockquote>
<p><code>a = a + b</code>和<code>a += b</code>运行结果一样，但后者效率高于前者。</p>
</blockquote>
<h2 id="逻辑运算符"><a href="#逻辑运算符" class="headerlink" title="逻辑运算符"></a>逻辑运算符</h2><p>逻辑运算符用于数据间逻辑真假的判断。JavaScript 中的逻辑运算符分为三种：</p>
<ul>
<li>“与”运算符<code>&amp;&amp;</code>：<strong>找假</strong></li>
<li>“或”运算符<code>||</code>：<strong>找真</strong></li>
<li>“非”运算符<code>!</code>：<strong>取反</strong></li>
</ul>
<figure class="highlight js"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line"><span class="built_in">console</span>.log(<span class="number">0</span> || <span class="string">''</span> || <span class="literal">false</span> || <span class="literal">null</span> || <span class="number">2</span> || <span class="literal">undefined</span>);    <span class="comment">// 2</span></div><div class="line"><span class="built_in">console</span>.log(<span class="number">10</span> &gt; <span class="number">5</span> &amp;&amp; <span class="number">10</span> &lt; <span class="number">20</span>);   <span class="comment">// true</span></div><div class="line"><span class="built_in">console</span>.log(!<span class="number">10</span>);   <span class="comment">// false</span></div></pre></td></tr></table></figure>
]]></content>
    
    <summary type="html">
    
      &lt;p&gt;JavaScript 中提供了大量的运算符用于数据的操作。&lt;/p&gt;
    
    </summary>
    
      <category term="JavaScript" scheme="http://yoursite.com/categories/JavaScript/"/>
    
    
      <category term="运算符" scheme="http://yoursite.com/tags/%E8%BF%90%E7%AE%97%E7%AC%A6/"/>
    
  </entry>
  
  <entry>
    <title>数据类型转换</title>
    <link href="http://yoursite.com/2018/02/24/changeDataType/"/>
    <id>http://yoursite.com/2018/02/24/changeDataType/</id>
    <published>2018-02-24T02:00:00.000Z</published>
    <updated>2018-08-21T02:29:03.730Z</updated>
    
    <content type="html"><![CDATA[<p>JavaScript 是一种动态数据类型语言。变量是没有类型的，可以随机被赋予任意类型的值。而数据本身和各种运算是有类型的，例如在做数学运算时我们就需要数据是 number 类型，但有时候实际参与数学运算的数据并不全是 number 类型，因此在进行某些运算时数据是需要转换类型的。大多数情况下，这种数据类型转换是自动的，但是有时也需要手动强制转换。</p>
<a id="more"></a>
<h2 id="显示转换"><a href="#显示转换" class="headerlink" title="显示转换"></a>显示转换</h2><p>显示转换，即通过调用一些函数方法进行强制转换。JavaScript 提供了以下几种转型函数：</p>
<ul>
<li>转换为数值类型：<code>parseInt()</code>、<code>parseFloat()</code>、<code>Number()</code></li>
<li>转换为字符串类型：<code>toString()</code>、<code>String()</code></li>
<li>转换为布尔值类型：<code>Boolean()</code></li>
</ul>
<h3 id="转换为数值类型"><a href="#转换为数值类型" class="headerlink" title="转换为数值类型"></a>转换为数值类型</h3><p><strong><code>parseInt()</code></strong></p>
<p>该方法用于解析一个字符串，并返回一个 number 类型的整数。解析规则如下：</p>
<ol>
<li>忽略字符串前面的空格，直至找到第一个非空字符；</li>
<li>如果第一个字符是 0，则忽略；</li>
<li>如果第一个字符不是数字或者正负号，返回<code>NaN</code>；</li>
<li>如果第一个字符是数字，则继续解析直到字符串解析完毕或者遇到一个非数字字符为止；</li>
<li>如果是以 0x 开头，则将其当作十六进制来解析；</li>
</ol>
<figure class="highlight js"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div></pre></td><td class="code"><pre><div class="line"><span class="built_in">parseInt</span>(<span class="string">'12345px'</span>);          <span class="comment">// 12345</span></div><div class="line"><span class="built_in">parseInt</span>(<span class="string">'hello parseInt'</span>);   <span class="comment">// NaN</span></div><div class="line"><span class="built_in">parseInt</span>(<span class="string">''</span>);                 <span class="comment">// NaN</span></div><div class="line"><span class="built_in">parseInt</span>(<span class="string">'020'</span>);              <span class="comment">// 20</span></div><div class="line"><span class="built_in">parseInt</span>(<span class="string">'11.22.33'</span>);         <span class="comment">// 11</span></div><div class="line"><span class="built_in">parseInt</span>(<span class="number">123.3</span>);              <span class="comment">// 123</span></div></pre></td></tr></table></figure>
<p><strong><code>parseFloat()</code></strong></p>
<p>该方法用于解析一个字符串，并返回一个 number 类型的浮点数（小数）。解析规则与<code>parseInt()</code>基本相同，但也有点区别：</p>
<ol>
<li>解析到的第一个小数点<code>.</code>符号是有效的；</li>
<li>如果字符串包含一个可解析为整数的数，则返回整数值而不是浮点数值；</li>
</ol>
<figure class="highlight js"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div></pre></td><td class="code"><pre><div class="line"><span class="built_in">parseFloat</span>(<span class="string">'12345px'</span>);          <span class="comment">// 12345</span></div><div class="line"><span class="built_in">parseFloat</span>(<span class="string">'hello parseInt'</span>);   <span class="comment">// NaN</span></div><div class="line"><span class="built_in">parseFloat</span>(<span class="string">''</span>);                 <span class="comment">// NaN</span></div><div class="line"><span class="built_in">parseFloat</span>(<span class="string">'020'</span>);              <span class="comment">// 20</span></div><div class="line"><span class="built_in">parseFloat</span>(<span class="string">'11.22.33'</span>);         <span class="comment">// 11.22</span></div><div class="line"><span class="built_in">parseFloat</span>(<span class="number">123.3</span>);              <span class="comment">// 123.2</span></div></pre></td></tr></table></figure>
<blockquote>
<p><code>parseInt()</code>和<code>parseFloat()</code>的解析规则可以简单的理解为：从左往右依次解析，结果保留前面合法的，裁剪去后面不合法的。如果第一个就不合法，结果则为<code>NaN</code>。</p>
</blockquote>
<p><strong><code>Number()</code></strong></p>
<p>该方法用于解析任意类型数据，并返回一个有效的 number 类型的值。解析规则如下：</p>
<table>
<thead>
<tr>
<th>数据类型</th>
<th>解析规则</th>
</tr>
</thead>
<tbody>
<tr>
<td>布尔值</td>
<td>1. <code>true</code>转换为 1<br>2. <code>false</code>转换为 0</td>
</tr>
<tr>
<td>null</td>
<td>转换为 0</td>
</tr>
<tr>
<td>undefined</td>
<td>转换为<code>NaN</code></td>
</tr>
<tr>
<td>字符串</td>
<td>1. 空字符串<code>&quot;&quot;</code>转换为 0<br>2. 如果引号内整体为一个有效数字，则转换为对应的数字<br>3. 其他情况都转换为<code>NaN</code></td>
</tr>
</tbody>
</table>
<figure class="highlight js"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div></pre></td><td class="code"><pre><div class="line"><span class="built_in">Number</span>(<span class="string">'12345px'</span>);          <span class="comment">// NaN</span></div><div class="line"><span class="built_in">Number</span>(<span class="string">'hello Number'</span>);     <span class="comment">// NaN</span></div><div class="line"><span class="built_in">Number</span>(<span class="string">''</span>);                 <span class="comment">// 0</span></div><div class="line"><span class="built_in">Number</span>(<span class="string">'020'</span>);              <span class="comment">// 20</span></div><div class="line"><span class="built_in">Number</span>(<span class="string">'11.22.33'</span>);         <span class="comment">// NaN</span></div><div class="line"><span class="built_in">Number</span>(<span class="number">123.3</span>);              <span class="comment">// 123.2</span></div></pre></td></tr></table></figure>
<h3 id="转换为字符串类型"><a href="#转换为字符串类型" class="headerlink" title="转换为字符串类型"></a>转换为字符串类型</h3><p><strong><code>toString()</code></strong></p>
<p>该方法用于解析除了 undefined 和 null 以外的任意类型数据，并返回一个 string 类型的值。</p>
<figure class="highlight js"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">let</span> data = <span class="literal">true</span>;</div><div class="line">data.toString();      <span class="comment">// 'true'</span></div></pre></td></tr></table></figure>
<p><strong><code>String()</code></strong></p>
<p>该方法用于解析任意类型数据，并返回一个 string 类型的值。</p>
<figure class="highlight js"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">let</span> data = <span class="literal">null</span>;</div><div class="line"><span class="built_in">String</span>(data);      <span class="comment">// 'null'</span></div></pre></td></tr></table></figure>
<h3 id="转换为布尔值类型"><a href="#转换为布尔值类型" class="headerlink" title="转换为布尔值类型"></a>转换为布尔值类型</h3><p><strong><code>Boolean()</code></strong></p>
<p>该方法用于解析任意类型数据，并返回一个 boolean 类型的值。解析规则如下：</p>
<ul>
<li><code>false</code></li>
<li><code>null</code></li>
<li><code>undefined</code></li>
<li><code>0</code></li>
<li><code>NaN</code></li>
<li><code>&quot;&quot;</code></li>
</ul>
<p>除了以上六种数据转换结果为<code>false</code>外，其他所有数据转换结果都为<code>true</code>。</p>
<figure class="highlight js"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line"><span class="built_in">Boolean</span>(<span class="number">0</span>);    <span class="comment">// false</span></div><div class="line"><span class="built_in">Boolean</span>(<span class="number">1</span>);    <span class="comment">// true</span></div></pre></td></tr></table></figure>
<h2 id="隐式转换"><a href="#隐式转换" class="headerlink" title="隐式转换"></a>隐式转换</h2><p>在 JavaScript 中很多操作都会存在数据自动转换的情况。即当参与操作的数据并不是预期的类型时，程序会对数据进行隐式转换。</p>
<blockquote>
<p>注意：隐式转换是得到一个转换后的新数据，不会改变原数据。</p>
</blockquote>
<p>以下列举几种常见的隐式转换的情况：</p>
<h3 id="转换为数字类型"><a href="#转换为数字类型" class="headerlink" title="转换为数字类型"></a>转换为数字类型</h3><p><strong>进行算术运算</strong></p>
<p>当对数据进行数学运算操作（加减乘除等）时，如果参与运算的数据不是 number 类型，则会将数据<strong>隐式的调用<code>Number()</code>方法</strong>转为数字后再进行运算。</p>
<figure class="highlight js"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">let</span> data1 = <span class="literal">null</span>;</div><div class="line"><span class="keyword">let</span> data2 = <span class="literal">true</span>;</div><div class="line"><span class="built_in">console</span>.log(<span class="number">10</span> - data2);      <span class="comment">// 9</span></div><div class="line"><span class="built_in">console</span>.log(data1 + data2);   <span class="comment">// 1</span></div><div class="line"><span class="built_in">console</span>.log(data2 * <span class="number">10</span>);      <span class="comment">// 10</span></div><div class="line"><span class="built_in">console</span>.log(--data1);         <span class="comment">// -1</span></div></pre></td></tr></table></figure>
<blockquote>
<p>小技巧：给任意数据减去数字 0，可快速将当前数据转为 number 类型。</p>
</blockquote>
<p><strong>进行大小比较</strong></p>
<p>当对数据进行大小比较时（大于小于等于等）时，如果参与比较的数据不能直接进行比较，则会将数据<strong>隐式的调用<code>Number</code></strong>转为数字后再进行比较。</p>
<figure class="highlight js"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line"><span class="built_in">console</span>.log(<span class="literal">true</span> == <span class="number">1</span>);</div><div class="line"><span class="built_in">console</span>.log(<span class="literal">true</span> == <span class="number">2</span>);</div><div class="line"><span class="built_in">console</span>.log(<span class="number">123</span> &gt; <span class="string">'13'</span>);</div><div class="line"><span class="built_in">console</span>.log(<span class="literal">null</span> &lt; <span class="literal">true</span>);</div></pre></td></tr></table></figure>
<p><strong><code>isNaN()</code></strong></p>
<p><code>isNaN()</code>方法用于判断数据是否为“非数字”。但是在判断前会先将数据<strong>隐式的调用<code>Number()</code>方法</strong>转为数字后再进行判断。如果转换后的数据为“非数字”，则返回结果<code>true</code>，反之则<code>false</code>。 </p>
<figure class="highlight js"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div></pre></td><td class="code"><pre><div class="line"><span class="built_in">console</span>.log(<span class="built_in">isNaN</span>(<span class="string">'123'</span>));        <span class="comment">// false</span></div><div class="line"><span class="built_in">console</span>.log(<span class="built_in">isNaN</span>(<span class="string">'123px'</span>));      <span class="comment">// true</span></div><div class="line"><span class="built_in">console</span>.log(<span class="built_in">isNaN</span>(<span class="literal">undefined</span>));    <span class="comment">// true</span></div><div class="line"><span class="built_in">console</span>.log(<span class="built_in">isNaN</span>(<span class="literal">false</span>));        <span class="comment">// false</span></div><div class="line"><span class="built_in">console</span>.log(<span class="built_in">isNaN</span>(<span class="literal">null</span>));         <span class="comment">// false</span></div></pre></td></tr></table></figure>
<h3 id="转换为字符串类型-1"><a href="#转换为字符串类型-1" class="headerlink" title="转换为字符串类型"></a>转换为字符串类型</h3><p>加号<code>+</code>除了用于数学加法运算外，还可以进行字符串的拼接。因此，当参与<code>+</code>操作的数据中存在一个 string 类型的值时，其他数据都会<strong>隐式的调用<code>String()</code>方法</strong>转换为字符串后进行拼接。</p>
<figure class="highlight js"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line"><span class="built_in">console</span>.log(<span class="number">1</span> + <span class="string">'2'</span>);       <span class="comment">// '12'</span></div><div class="line"><span class="built_in">console</span>.log(<span class="string">'10'</span> + <span class="literal">true</span>);   <span class="comment">// '10true'</span></div></pre></td></tr></table></figure>
<blockquote>
<p>小技巧：给任意数据拼接一个空字符串，可快速将当前数据转为 string 类型且不改变数据值。</p>
</blockquote>
<figure class="highlight js"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line"><span class="built_in">console</span>.log(<span class="number">10</span> + <span class="string">''</span>);     <span class="comment">// '10'</span></div></pre></td></tr></table></figure>
<h3 id="转换为布尔值类型-1"><a href="#转换为布尔值类型-1" class="headerlink" title="转换为布尔值类型"></a>转换为布尔值类型</h3><p>当进行逻辑判断时，如果参与判断的数据不是布尔值类型，则会<strong>隐式的调用<code>Boolean()</code>方法</strong>转换为布尔值。</p>
<figure class="highlight js"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line"><span class="built_in">console</span>.log(<span class="number">1</span> &amp;&amp; <span class="number">0</span>);   <span class="comment">// 0</span></div><div class="line"><span class="keyword">if</span>(<span class="string">''</span>)&#123;   </div><div class="line">	<span class="built_in">console</span>.log(<span class="string">'hello'</span>);   <span class="comment">// 不会执行</span></div><div class="line">&#125;</div></pre></td></tr></table></figure>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;JavaScript 是一种动态数据类型语言。变量是没有类型的，可以随机被赋予任意类型的值。而数据本身和各种运算是有类型的，例如在做数学运算时我们就需要数据是 number 类型，但有时候实际参与数学运算的数据并不全是 number 类型，因此在进行某些运算时数据是需要转换类型的。大多数情况下，这种数据类型转换是自动的，但是有时也需要手动强制转换。&lt;/p&gt;
    
    </summary>
    
      <category term="JavaScript" scheme="http://yoursite.com/categories/JavaScript/"/>
    
    
      <category term="数据类型转换" scheme="http://yoursite.com/tags/%E6%95%B0%E6%8D%AE%E7%B1%BB%E5%9E%8B%E8%BD%AC%E6%8D%A2/"/>
    
  </entry>
  
  <entry>
    <title>数据类型</title>
    <link href="http://yoursite.com/2018/02/12/dataType/"/>
    <id>http://yoursite.com/2018/02/12/dataType/</id>
    <published>2018-02-12T02:00:00.000Z</published>
    <updated>2018-08-21T02:28:49.521Z</updated>
    
    <content type="html"><![CDATA[<p>在<a href="https://runjaelyn.github.io/2018/01/10/variable/" target="_blank" rel="external">《变量和常量》</a>一章中我们知道，开发者为了能够更方便的操作数据，会将数据进行命名保存。但是，仅仅是对每一条数据进行命名保存是不够的。因为在开发过程中，往往会涉及到大量的数据，任何东西一旦数量多了之后就会变得难以管理。所以，除了对数据进行命名保存外，开发者们还对所有的数据都进行了分类，让程序中用到的任何一条数据都是属于某一类数据类型的。</p>
<a id="more"></a>
<h2 id="数据类型的分类"><a href="#数据类型的分类" class="headerlink" title="数据类型的分类"></a>数据类型的分类</h2><p>截止到 ES6 为止，JavaScript 将所有的数据划分为了 7 种不同的数据类型。其中 6 种属于简单数据类型，1 种属于复杂数据类型。</p>
<table>
<thead>
<tr>
<th>数据类型</th>
<th>说明</th>
</tr>
</thead>
<tbody>
<tr>
<td>number</td>
<td>数值</td>
</tr>
<tr>
<td>string</td>
<td>字符串</td>
</tr>
<tr>
<td>boolean</td>
<td>布尔值</td>
</tr>
<tr>
<td>undefined</td>
<td>未定义</td>
</tr>
<tr>
<td>null</td>
<td>空值</td>
</tr>
<tr>
<td>symbol</td>
<td>符号</td>
</tr>
<tr>
<td>object</td>
<td>对象</td>
</tr>
</tbody>
</table>
<h2 id="简单数据类型"><a href="#简单数据类型" class="headerlink" title="简单数据类型"></a>简单数据类型</h2><p>简单数据类型也称之为“基础数据类型“、”原始数据类型”。</p>
<h3 id="number"><a href="#number" class="headerlink" title="number"></a>number</h3><p>number 类型即“数值类型”。我们数学中用到的所有数字都属于 number 类型。例如：</p>
<figure class="highlight js"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line"><span class="number">100</span>;   <span class="comment">// 整数</span></div><div class="line"><span class="number">0.1</span>;   <span class="comment">// 小数</span></div><div class="line"><span class="number">-50</span>;   <span class="comment">// 负数</span></div></pre></td></tr></table></figure>
<p>JavaScript 的 number 类型中除了数学中的数字外，还有一个特殊值 —— <code>NaN</code>。</p>
<p><code>NaN</code>（Not a Number），表示“非数字”。它的特殊之处不仅仅在于“非数字”属于数值类型，<code>NaN</code>还有两个特点：</p>
<ol>
<li>任何跟<code>NaN</code>有关的操作，得到的结果都是<code>NaN</code>。</li>
<li><code>NaN</code>跟任何数据都不相等，包括<code>NaN</code>自己。</li>
</ol>
<figure class="highlight js"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line"><span class="built_in">console</span>.log(<span class="literal">NaN</span> - <span class="number">10</span>);     <span class="comment">// NaN</span></div><div class="line"><span class="built_in">console</span>.log(<span class="literal">NaN</span> == <span class="literal">NaN</span>);   <span class="comment">// false（JavaScript 中判断两个值是否相等用“==”，后面会详讲）</span></div></pre></td></tr></table></figure>
<h3 id="string"><a href="#string" class="headerlink" title="string"></a>string</h3><p>string 类型即“字符串类型”。任何数据只要被引号包起来，那么就属于 string 类型。例如：</p>
<figure class="highlight js"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line"><span class="string">"hello"</span>;</div><div class="line"><span class="string">'123'</span>;</div></pre></td></tr></table></figure>
<blockquote>
<p> 注意：在 JavaScript 中单双引号没有区别。但是同类型引号不能互相包含。</p>
</blockquote>
<figure class="highlight js"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div></pre></td><td class="code"><pre><div class="line"><span class="comment">// 错误语法</span></div><div class="line"><span class="string">"hello, "</span>JavaScript<span class="string">""</span>;</div><div class="line"><span class="comment">// 正确语法</span></div><div class="line"><span class="string">"hello, my name is 'HanMeiMei'"</span>;</div><div class="line"><span class="string">'this is an "apple"'</span>;</div></pre></td></tr></table></figure>
<h4 id="模板字符串"><a href="#模板字符串" class="headerlink" title="模板字符串"></a>模板字符串</h4><p>除了常见的单双引号外，ES6 中还新增了一种字符串的表现形式，叫做“模板字符串”。符号为 ``（与波浪号同一按键）。</p>
<figure class="highlight js"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line"><span class="string">`hello JavaScript`</span>;</div></pre></td></tr></table></figure>
<p>模板字符串与普通字符串的区别：</p>
<ol>
<li><p>模板字符串内部支持回车换行</p>
<figure class="highlight"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div></pre></td><td class="code"><pre><div class="line">// 错误语法（普通字符串内部不支持回车换行）</div><div class="line">let str1 = "how are you?</div><div class="line">		   I'm fine, thank you.";</div><div class="line">// 正确语法</div><div class="line">let str2 = `how are you?</div><div class="line">		   I'm fine, thank you.`;</div></pre></td></tr></table></figure>
</li>
<li><p>可以解析模板字符串内部代码</p>
<figure class="highlight js"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">let</span> name = <span class="string">"韩梅梅"</span>;</div><div class="line"><span class="keyword">let</span> str1 = <span class="string">"hello, name"</span>;     <span class="comment">// hello, name</span></div><div class="line"><span class="comment">// 在模板字符串中，将需要解析的代码用 $&#123;&#125; 包起来</span></div><div class="line"><span class="keyword">let</span> str2 = <span class="string">`hello, <span class="subst">$&#123;name&#125;</span>`</span>;  <span class="comment">// hello, 韩梅梅</span></div></pre></td></tr></table></figure>
</li>
</ol>
<h3 id="boolean"><a href="#boolean" class="headerlink" title="boolean"></a>boolean</h3><p>boolean 类型即“布尔值类型”。这个类型中只有两条数据：<code>true</code>和<code>false</code>，分别表示“真”和“假”。</p>
<h3 id="undefined"><a href="#undefined" class="headerlink" title="undefined"></a>undefined</h3><p>undefined 类型即“未定义类型”。这个类型中只有一条数据：<code>undefined</code>。</p>
<p><code>undefined</code>这个值一般用于表示”没有数据”。当没有数据而程序又在访问数据时，就会得到<code>undefined</code>这个值。</p>
<p>可以简单的理解为：“有容器，没数据”。即当容器创建好，但容器中并没有保存数据时，这个时候就默认容器中的数据值为<code>undefined</code> 。内存会为<code>undefined</code>分配储存空间。</p>
<figure class="highlight js"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">let</span> num;</div><div class="line"><span class="built_in">console</span>.log(num);  <span class="comment">// undefined</span></div></pre></td></tr></table></figure>
<h3 id="null"><a href="#null" class="headerlink" title="null"></a>null</h3><p>null 类型即“空值类型”。这个类型中也只有一条数据：<code>null</code>。</p>
<p><code>null</code>这个值用于表示”没有/空”，一般用于释放内存。当一个容器的数据值被设置为<code>null</code>后，内存中就不再为该数据分配储存空间。</p>
<figure class="highlight js"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">let</span> num = <span class="number">1000</span>;</div><div class="line">num = <span class="literal">null</span>;</div></pre></td></tr></table></figure>
<h3 id="symbol"><a href="#symbol" class="headerlink" title="symbol"></a>symbol</h3><p>symbol 类型即符号类型。这个类型是 ES6 中新增的一种数据类型，用来表示唯一的值。即在这个类型下的所有数据都是不相等的。</p>
<h2 id="复杂数据类型"><a href="#复杂数据类型" class="headerlink" title="复杂数据类型"></a>复杂数据类型</h2><p>复杂数据类型也称之为“引用数据类型”。JavaScript 中的复杂数据类型只有一种：object。</p>
<h3 id="object"><a href="#object" class="headerlink" title="object"></a>object</h3><p>object 类型即对象类型。在 JavaScript 中，只要是不属于 6 种简单类型的数据，都可以看做属于复杂类型。</p>
<p>例如后面我们会讲到的<code>Array</code>、<code>Set</code>、<code>Map</code>等。</p>
]]></content>
    
    <summary type="html">
    
      &lt;p&gt;在&lt;a href=&quot;https://runjaelyn.github.io/2018/01/10/variable/&quot; target=&quot;_blank&quot; rel=&quot;external&quot;&gt;《变量和常量》&lt;/a&gt;一章中我们知道，开发者为了能够更方便的操作数据，会将数据进行命名保存。但是，仅仅是对每一条数据进行命名保存是不够的。因为在开发过程中，往往会涉及到大量的数据，任何东西一旦数量多了之后就会变得难以管理。所以，除了对数据进行命名保存外，开发者们还对所有的数据都进行了分类，让程序中用到的任何一条数据都是属于某一类数据类型的。&lt;/p&gt;
    
    </summary>
    
      <category term="JavaScript" scheme="http://yoursite.com/categories/JavaScript/"/>
    
    
      <category term="简单数据类型" scheme="http://yoursite.com/tags/%E7%AE%80%E5%8D%95%E6%95%B0%E6%8D%AE%E7%B1%BB%E5%9E%8B/"/>
    
      <category term="复杂数据类型" scheme="http://yoursite.com/tags/%E5%A4%8D%E6%9D%82%E6%95%B0%E6%8D%AE%E7%B1%BB%E5%9E%8B/"/>
    
  </entry>
  
  <entry>
    <title>变量和常量</title>
    <link href="http://yoursite.com/2018/02/10/variable/"/>
    <id>http://yoursite.com/2018/02/10/variable/</id>
    <published>2018-02-10T02:00:00.000Z</published>
    <updated>2018-08-21T02:28:42.081Z</updated>
    
    <content type="html"><![CDATA[<p>任何程序的运行的都离不开数据。同理，任何程序的开发也离不开数据。不管是日常的代码练习还是实际的项目开发，都会涉及到数据的操作（例如：数据的增删改查）。为了能够更方便的去操作数据，开发者们对数据进行了命名保存，即把需要操作的数据保存在一个有名字的容器中。</p>
<p>在 JavaScript 中，变量和常量就可以看做是“有名字的保存数据的容器”。</p>
<a id="more"></a>
<h2 id="变量的声明"><a href="#变量的声明" class="headerlink" title="变量的声明"></a>变量的声明</h2><p>想要保存数据，首先需要将容器创建出来。我们将创建容器的过程称之为“变量的声明”。</p>
<p>变量的声明方式总共分为两种：</p>
<ol>
<li><code>var</code>：ES6 出来之前最常用的声明变量的方式。</li>
<li><code>let</code>：ES6 中新增的声明变量的方式。</li>
</ol>
<p>两种声明方式的语法结构如下：</p>
<figure class="highlight js"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">var</span> 变量名;</div><div class="line"><span class="keyword">let</span> 变量名;</div></pre></td></tr></table></figure>
<p>例如，我们要通过<code>var</code>声明一个名字叫做<code>num</code>的变量，代码如下：</p>
<figure class="highlight js"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">var</span> num;</div></pre></td></tr></table></figure>
<p>同理，如果我们要通过<code>let</code>声明一个名字叫做<code>username</code>的变量，代码如下：</p>
<figure class="highlight js"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">let</span> username;</div></pre></td></tr></table></figure>
<h2 id="变量的赋值"><a href="#变量的赋值" class="headerlink" title="变量的赋值"></a>变量的赋值</h2><p>容器创建好之后，就可以将数据保存到容器里了。我们把数据保存进容器的过程称之为“变量的赋值”。</p>
<p><code>var</code>和<code>let</code>的赋值方式一样，语法结构如下：</p>
<figure class="highlight js"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">变量名 = 数据;</div></pre></td></tr></table></figure>
<blockquote>
<p>注意：JavaScript 中的<code>=</code>和数学中的<code>=</code>含义并不相同。在 JavaScript 中，<code>=</code>叫做赋值符，表示将<code>=</code>右边的数据赋值给<code>=</code>左边的变量。</p>
</blockquote>
<p>例如：我们要将数字<code>100</code>赋值给变量<code>num</code>。代码如下：</p>
<figure class="highlight js"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">var</span> num;</div><div class="line">num = <span class="number">100</span>;</div></pre></td></tr></table></figure>
<p>又例如：我们要将姓名<code>John</code>赋值给变量<code>name</code>。代码如下：</p>
<figure class="highlight js"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">let</span> username;</div><div class="line">username = <span class="string">'John'</span>;</div></pre></td></tr></table></figure>
<p>同一个变量可以进行多次赋值，后面赋值的数据会将前面赋值的数据覆盖掉，以此来实现变量值的修改。例如：</p>
<figure class="highlight js"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">let</span> username;</div><div class="line">username = <span class="string">'John'</span>;   <span class="comment">// 第一次赋值</span></div><div class="line">username = <span class="string">'Lucy'</span>;   <span class="comment">// 第二次赋值</span></div></pre></td></tr></table></figure>
<p>上面代码中，变量<code>username</code>最终保存的数据为<code>Lucy</code>。</p>
<h2 id="变量的初始化"><a href="#变量的初始化" class="headerlink" title="变量的初始化"></a>变量的初始化</h2><p>我们可以先创建容器，再保存数据。也可以在创建容器的同时就将数据保存进去。即声明变量的同时就对变量进行赋值，我们将这个声明和赋值同时进行的过程称之为“变量的初始化”。</p>
<p>我们将上面两个例子改写成变量初始化的代码：</p>
<figure class="highlight js"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">var</span> num = <span class="number">100</span>;</div><div class="line"><span class="keyword">let</span> username = <span class="string">'John'</span>;</div></pre></td></tr></table></figure>
<h2 id="常量的初始化"><a href="#常量的初始化" class="headerlink" title="常量的初始化"></a>常量的初始化</h2><p>保存数据的容器除了变量之外还有常量。在 ES6 出来之前，并没有专门声明常量的方式。ES6 中才新增了关键字<code>const</code>用来声明常量。</p>
<p>常量的使用要求在声明的同时必须赋值，否则程序会报错。</p>
<figure class="highlight js"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div></pre></td><td class="code"><pre><div class="line"><span class="comment">// 错误语法</span></div><div class="line"><span class="keyword">const</span> num;</div><div class="line">num = <span class="number">10</span>;</div><div class="line"><span class="comment">// 正确语法</span></div><div class="line"><span class="keyword">const</span> num = <span class="number">10</span>;</div></pre></td></tr></table></figure>
<blockquote>
<p> 注意：常量一旦被初始化，值就不能被改变。常量的使用只能进行读取操作。</p>
</blockquote>
<figure class="highlight js"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">const</span> num = <span class="number">10</span>;</div><div class="line">num = <span class="number">20</span>;   <span class="comment">// 报错，常量不允许修改</span></div></pre></td></tr></table></figure>
]]></content>
    
    <summary type="html">
    
      &lt;p&gt;任何程序的运行的都离不开数据。同理，任何程序的开发也离不开数据。不管是日常的代码练习还是实际的项目开发，都会涉及到数据的操作（例如：数据的增删改查）。为了能够更方便的去操作数据，开发者们对数据进行了命名保存，即把需要操作的数据保存在一个有名字的容器中。&lt;/p&gt;
&lt;p&gt;在 JavaScript 中，变量和常量就可以看做是“有名字的保存数据的容器”。&lt;/p&gt;
    
    </summary>
    
      <category term="JavaScript" scheme="http://yoursite.com/categories/JavaScript/"/>
    
    
      <category term="let" scheme="http://yoursite.com/tags/let/"/>
    
      <category term="var" scheme="http://yoursite.com/tags/var/"/>
    
      <category term="const" scheme="http://yoursite.com/tags/const/"/>
    
  </entry>
  
  <entry>
    <title>JavaScript 内存空间</title>
    <link href="http://yoursite.com/2018/01/19/memory/"/>
    <id>http://yoursite.com/2018/01/19/memory/</id>
    <published>2018-01-19T04:30:10.000Z</published>
    <updated>2018-08-21T02:28:32.440Z</updated>
    
    <content type="html"><![CDATA[<p>程序的运行需要内存。只要程序提出要求，操作系统或者运行时（runtime）就必须供给内存。</p>
<p>但是对于前端开发来说，内存并不是一个经常被提及的概念，很容易被大家忽视。可如果想要对 JavaScript 的理解更加深刻，就必须对内存有一个清晰的认知。</p>
<a id="more"></a>
<h2 id="内存生命周期"><a href="#内存生命周期" class="headerlink" title="内存生命周期"></a>内存生命周期</h2><p>在 JavaScript 中内存的生命周期分为三个阶段：</p>
<ol>
<li>分配你所需要的内存</li>
<li>使用（读/写）分配到的内存 </li>
<li>不需要时将内存释放、归还</li>
</ol>
<p>为了便于理解，我们使用一个简单的例子来解释这个周期。</p>
<figure class="highlight js"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">let</span> a = <span class="number">20</span>;               <span class="comment">// 第一阶段：分配变量 a 所需要的空间</span></div><div class="line"><span class="built_in">console</span>.log(a + <span class="number">100</span>);     <span class="comment">// 第二阶段：使用分配到的内存空间</span></div><div class="line">a = <span class="literal">null</span>;                 <span class="comment">// 第三阶段：使用完毕之后，释放内存空间</span></div></pre></td></tr></table></figure>
<p>前两步我们都很好理解，JavaScript 在定义变量的时候就完成了内存分配。第三步释放内存空间则是我们需要重点理解的一个点。</p>
<h3 id="内存泄漏"><a href="#内存泄漏" class="headerlink" title="内存泄漏"></a>内存泄漏</h3><p>对于持续运行的服务进程（daemon），必须及时释放不再用到的内存。否则，内存占用越来越高，轻则影响系统性能，重则导致进程崩溃。</p>
<p>不再用到的内存，没有及时释放，就叫做“内存泄漏”（memory leak）。</p>
<h3 id="垃圾回收机制"><a href="#垃圾回收机制" class="headerlink" title="垃圾回收机制"></a>垃圾回收机制</h3><p>有些代码语言（比如 C 语言）必须手动释放内存，程序员负责内存管理。这样的话，对于程序员来说，其实是非常麻烦的。所以大多数代码语言（比如 JavaScript 和 C#，Java 等）都提供自动内存管理，以此来减轻程序员的负担。</p>
<p>这种自动内存管理的操作，我们称之为“垃圾回收机制”（garbage collector）。</p>
<blockquote>
<p>原理：垃圾收集器会定期（周期性）找出那些不再继续使用的数据，然后释放其所占内存。</p>
</blockquote>
<p>最常见的回收方式分为两种：标记清除和引入计数。</p>
<h2 id="堆和栈"><a href="#堆和栈" class="headerlink" title="堆和栈"></a>堆和栈</h2><p>堆（heap）和栈（stack）是程序运行时内存中分配出来的数据区，用于保存程序运行过程中用到的所有数据。</p>
<h3 id="基础数据类型与栈"><a href="#基础数据类型与栈" class="headerlink" title="基础数据类型与栈"></a>基础数据类型与栈</h3><p>JavaScript 有六种基础数据类型：<code>undefined</code>、<code>null</code>、<code>boolean</code>、<code>number</code>、<code>string</code>、<code>symbol</code>（ES6）。</p>
<p>JavaScript 中的基础数据类型，都是一些简单的数据段，这些值都有固定的大小，往往保存在栈内存中，由系统自动分配存储空间，我们可以直接操作保存在栈内存空间的值.</p>
<blockquote>
<p>结论：基础数据类型都是通过数据值进行访问。</p>
</blockquote>
<h3 id="引用数据类型与堆"><a href="#引用数据类型与堆" class="headerlink" title="引用数据类型与堆"></a>引用数据类型与堆</h3><p>JavaScript 中的引用数据类型，比如数组<code>Array</code>，他们值的大小是不固定的。所以<strong>引用数据类型的值保存在堆内存中</strong>。</p>
<p>但是 JavaScript 不允许直接操作堆内存空间里保存的数据。所以引用类型的数据除了将实际的值保存在堆空间中以外，还需要在栈空间中保存了当前数据的引用（这里的引用，我们可以粗浅地理解为数据在堆空间中的地址）。那么操作引用类型的数据时，实际上是先通过栈空间中的<strong>引用</strong>去找到堆空间中的数据然后才进行操作。</p>
<blockquote>
<p>结论：引用类型的值都是通过数据引用（地址）进行访问。</p>
</blockquote>
<h2 id="数据存取方式"><a href="#数据存取方式" class="headerlink" title="数据存取方式"></a>数据存取方式</h2><p>数据在堆内存和栈内存中的存取方式是不一样的。</p>
<h3 id="栈的数据存取"><a href="#栈的数据存取" class="headerlink" title="栈的数据存取"></a>栈的数据存取</h3><p>栈（stack）是系统自动分配的内存空间。它对数据的存取原则为“<strong>先进后出，后进先出</strong>”。</p>
<p>我们可以通过类比乒乓球盒子来分析。如下图左侧：</p>
<p><img src="https://ws3.sinaimg.cn/large/006tKfTcgy1fhkn1bcdozj31360p2tc1.jpg" alt=""></p>
<p>这种乒乓球的存放方式与栈中存取数据的方式如出一辙。</p>
<p>处于盒子中最顶层的乒乓球E，它一定是最后被放进去的，但可以最先被使用。而我们如果想要使用底层的乒乓球A，就必须将上面的4个乒乓球都取出来，让乒乓球A处于盒子开口处，这样才能拿到乒乓球A。</p>
<p>栈内存空间中的数据也一样，处于栈底部的数据<code>1</code>一定是最先保存的，处于栈顶部的数据<code>true</code>一定是最后保存的。我们只能操作处于栈顶部的数据，当垃圾回收机制要开始销毁栈内存中无用的数据时，也只能从栈顶部开始销毁。</p>
<h3 id="堆的数据存取"><a href="#堆的数据存取" class="headerlink" title="堆的数据存取"></a>堆的数据存取</h3><p>堆（heap）是在程序运行时动态分配的内存空间。堆存取数据的方式，则与书架和书非常相似。</p>
<p>对于存放在书架上的书来说，我们并不需要记住每一本放进书架的先后顺序。我们只要知道书的名字，就可以很方便的取出我们想要的书，而不用像从乒乓球盒子里取乒乓球一样，非得将上面的所有乒乓球都拿出来才能取到下面的某一个乒乓球。</p>
<p>而堆存取数据，也就是这个特点，我们只需要知道堆中每一条数据的引用（地址），就可以通过引用（地址）找到对应的数据，而不用去关心数据存进堆中的先后顺序。</p>
<h2 id="图解举例"><a href="#图解举例" class="headerlink" title="图解举例"></a>图解举例</h2><p>为了更好的搞懂栈内存与堆内存，我们可以结合以下例子与图解进行理解。</p>
<figure class="highlight js"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">var</span> a = <span class="number">0</span>;   <span class="comment">// 栈</span></div><div class="line"><span class="keyword">var</span> b = <span class="string">"hello world"</span>;   <span class="comment">// 栈</span></div><div class="line"><span class="keyword">var</span> c = <span class="literal">null</span>;   <span class="comment">// 栈</span></div><div class="line">	</div><div class="line"><span class="keyword">var</span> obj = &#123; <span class="attr">m</span>: <span class="number">20</span> &#125;;   <span class="comment">// 变量 obj 存在于栈中，&#123; m: 20 &#125; 作为对象存在于堆内存中</span></div><div class="line"><span class="keyword">var</span> arr = [<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>];   <span class="comment">// 变量 arr 存在于栈中，[1, 2, 3] 作为对象存在于堆内存中</span></div></pre></td></tr></table></figure>
<p>图解：</p>
<p><img src="https://ws1.sinaimg.cn/large/006tKfTcgy1fhknbpz837j30za0j0q56.jpg" alt=""></p>
<blockquote>
<p>因此，当我们要访问堆内存中的引用数据类型（如对象，数组，函数等）时，实际上我们首先是从栈中获取该对象的地址引用（或者地址指针），然后再通过这个地址从堆内存中取得我们需要的数据。</p>
</blockquote>
<h2 id="引用类型的特点"><a href="#引用类型的特点" class="headerlink" title="引用类型的特点"></a>引用类型的特点</h2><p>大致了解了 JavaScript 的内存空间，我们就可以借助内存空间的特性来验证一下引用类型的特点了。</p>
<p>例一：</p>
<figure class="highlight js"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">var</span> a = <span class="number">20</span>;</div><div class="line"><span class="keyword">var</span> b = a;  <span class="comment">// 将 a 的数据复制给 b</span></div><div class="line">b = <span class="number">30</span>;</div><div class="line"><span class="built_in">console</span>.log(a);</div></pre></td></tr></table></figure>
<p>图解：</p>
<p><img src="https://ws4.sinaimg.cn/large/006tKfTcgy1fhknn2es7ej311m0iydhx.jpg" alt=""></p>
<p>在栈内存中的数据发生复制行为时，系统会自动为新的变量分配一个新值。<code>var b = a</code> 执行之后，<code>a</code> 和 <code>b</code> 的值虽然都等于 20，但是他们其实已经是相互独立互不影响的值了。所以，我们修改了 <code>b</code> 的值以后，<code>a</code> 的值并不会发生变化。</p>
<p>例二：</p>
<figure class="highlight js"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">var</span> person1 = &#123; <span class="attr">name</span>: <span class="string">"zhangsan"</span>, <span class="attr">age</span>: <span class="number">20</span> &#125;;</div><div class="line"><span class="keyword">var</span> person2 = person1;  <span class="comment">// 将 person1 的值复制给 person2</span></div><div class="line">person2.name = <span class="string">"lisi"</span>;</div><div class="line"><span class="built_in">console</span>.log(person1.name);</div></pre></td></tr></table></figure>
<p>图解：</p>
<p><img src="https://ws1.sinaimg.cn/large/006tKfTcgy1fhkt5ff5syj30xe10etcp.jpg" alt=""></p>
<p>当我们通过 <code>var person2 = person1</code> 执行一次复制引用类型的操作时，引用类型的复制也会为新的变量分配一个新的值保存在栈内存中。但不同的是，这个新的值，仅仅只是引用类型的一个地址指针。当地址指针相同时，尽管他们相互独立，但是这两个地址指针指向的都是堆内存中的同一个地方，所以在堆内存中访问到的具体对象实际上是同一个。</p>
<p>因此，当我们改变 <code>person2</code> 的时候，就是在改变堆内存中 <code>person2</code> 指向的这个对象。而同时，<code>person1</code> 指向的也是这个对象，所以 <code>person1</code> 也会跟着发生变化。这就是引用类型的特性。</p>
<h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>我们通过一个表格对栈内存和堆内存做一个总结：</p>
<table>
<thead>
<tr>
<th>栈内存</th>
<th>堆内存</th>
</tr>
</thead>
<tbody>
<tr>
<td>保存基本数据类型</td>
<td>保存引用数据类型</td>
</tr>
<tr>
<td>通过值访问</td>
<td>通过地址访问</td>
</tr>
<tr>
<td>保存的值大小固定</td>
<td>保存的值大小不定，可动态调整</td>
</tr>
<tr>
<td>由系统自动分配内存</td>
<td>由程序员通过代码分配内存</td>
</tr>
<tr>
<td>主要用来执行程序</td>
<td>主要用来存放对象</td>
</tr>
<tr>
<td>空间小，运行效率高</td>
<td>空间大，运行效率较低</td>
</tr>
<tr>
<td>存取顺序为先进后出</td>
<td>无序存储，根据引用（地址）直接获取</td>
</tr>
</tbody>
</table>
]]></content>
    
    <summary type="html">
    
      &lt;p&gt;程序的运行需要内存。只要程序提出要求，操作系统或者运行时（runtime）就必须供给内存。&lt;/p&gt;
&lt;p&gt;但是对于前端开发来说，内存并不是一个经常被提及的概念，很容易被大家忽视。可如果想要对 JavaScript 的理解更加深刻，就必须对内存有一个清晰的认知。&lt;/p&gt;
    
    </summary>
    
    
      <category term="内存" scheme="http://yoursite.com/tags/%E5%86%85%E5%AD%98/"/>
    
      <category term="垃圾回收机制" scheme="http://yoursite.com/tags/%E5%9E%83%E5%9C%BE%E5%9B%9E%E6%94%B6%E6%9C%BA%E5%88%B6/"/>
    
      <category term="堆" scheme="http://yoursite.com/tags/%E5%A0%86/"/>
    
      <category term="栈" scheme="http://yoursite.com/tags/%E6%A0%88/"/>
    
  </entry>
  
  <entry>
    <title>浏览器发展史</title>
    <link href="http://yoursite.com/2018/01/09/browserHistory/"/>
    <id>http://yoursite.com/2018/01/09/browserHistory/</id>
    <published>2018-01-09T02:31:00.000Z</published>
    <updated>2018-08-21T02:45:17.335Z</updated>
    
    <content type="html"><![CDATA[<p>作为一个前端开发人员，经常与浏览器打交道，了解浏览器的一些相关历史也非常有必要的。</p>
<a id="more"></a>
<h2 id="导航者浏览器的诞生"><a href="#导航者浏览器的诞生" class="headerlink" title="导航者浏览器的诞生"></a>导航者浏览器的诞生</h2><p>最早一代的浏览器是什么其实没有人关心，毕竟在历史的长河中没有大作为的都是不重要的。而真正重要的一代浏览器，是网景公司（Netscape Corporation）在 1994 年底发布的 —— （Navigator）导航者浏览器（台湾译为领航员浏览器）1.0 版。</p>
<p>网景的创始人马克·安德森（Marc Andreessen）最早在伊利诺伊大学主修计算机专业，并在学校的国家超级计算机应用中心兼职。在那里，他与其他几位同伴共同开发了一款浏览器（就是后来网景导航者浏览器的原型），一上线就大受网民的欢迎。于是，马克·安德森决定在硅谷创业，开发浏览器。只经过了不到一年的时间，在 1994 年的年底，网景导航者浏览器从一发布就已经占领了浏览器市场占有率的首位。</p>
<p>因此在 1995 年，网景公司凭借着导航者浏览器成为了 Web 时代开启时最著名的第一代互联网公司。而他们公司就只做一款产品，就是浏览器。</p>
<h2 id="IE-浏览器的诞生"><a href="#IE-浏览器的诞生" class="headerlink" title="IE 浏览器的诞生"></a>IE 浏览器的诞生</h2><p>网景导航者浏览器的成功，让微软公司意识到了危机。所以微软为了应对网景的日益强大，直接购买了网景导航者浏览器原型版本的源代码（也就是马克·安德森在大学时期做的第一个版本的浏览器）。然后，稍作修改后就发布了。</p>
<p>因此，1995 年 8 月 16 日，微软推出了 IE 1.0 浏览器。同年 11 月发布了其第二个版本 IE 2.0。但是 IE 的这两个版本都几乎没有人使用，因为做的很烂。</p>
<p><img src="https://ws4.sinaimg.cn/large/006tKfTcgy1fml1ndc6n6j305k05k3ye.jpg" alt=""></p>
<p>不过在 IE3.0 的版本时，这种“极度嫌弃”的情况发生了质的改变。因为从 IE3.0 开始，微软将 IE 浏览器强行内置在了 Windows 的操作系统中。</p>
<p>同时，微软也在商业市场发力，联合大型互联网服务提供商，美国在线、AT&amp;T 等，让他们放弃网景导航者浏览器而改用 IE 作为首选浏览器。盖茨还曾经问过美国在线的高管，“微软需要出多少钱才能让你们放弃网景而改用 IE 作为推荐浏览器？”</p>
<h2 id="第一次浏览器大战"><a href="#第一次浏览器大战" class="headerlink" title="第一次浏览器大战"></a>第一次浏览器大战</h2><p>当微软在暗戳戳的到处下黑手的时候，网景在干什么呢？</p>
<p>在财力上拼不过别人的时候，网景当时就想要通过快速增加自己浏览器的功能来获取更多的安装量。但是，也正是因为大量新功能的增加，浏览器出现了性能不断下降、bug 频频出现、软件卡顿等等一系列问题，导致客户也逐渐失去了对导航者浏览器的信心。</p>
<p>最后，因为网景产品的问题，加上微软的围追堵截，网景的浏览器逐渐失去了市场份额，而 IE 浏览器基本垄断了浏览器的市场。从一开始 8% 的市场占有率上升到了 80% 多的市场占有率，以至于最后把网景公司给挤死了。</p>
<p>浏览器之间的第一次大战，最终以 IE 的胜利作为结束。在 1998 年，网景最后被美国在线（AOL）以 42 亿美元收购。</p>
<h2 id="火狐浏览器的诞生"><a href="#火狐浏览器的诞生" class="headerlink" title="火狐浏览器的诞生"></a>火狐浏览器的诞生</h2><p>网景公司难道甘心就这么被干掉吗？肯定不甘心啊，是你要是这么被搞死，你也要死前挣扎一下的吧。因此，网景公司在临死前开始搞事情，他们把公司唯一一款核心产品，也就是网景导航者浏览器的核心源代码开源了！！！开源，也就意味着，浏览器的源代码，你有我有大家都有了。</p>
<p><img src="https://ws2.sinaimg.cn/large/006tKfTcgy1fmkz98z7xej30ml0ett9y.jpg" alt=""></p>
<p>网景的一批老员工们在网景公司破产后，又一起成立了一个组织，计划重新开发下一代的网景导航者浏览器，这个组织就是 Mozilla.org，叫做 Mozilla。不过不久后，导航者浏览器的源代码就差不多被全部抛弃， Mozilla 组织开发出了在稳定性和功能上更出色的新一代浏览器。这个浏览器就是 Mozilla Application Suite，简称 Mozilla。</p>
<p>2002 年的时候，它们又从 Mozilla 浏览器项目中分支出来的一个实验性的项目，希望能够做出更轻便、快速、简单与高扩充性的浏览器。在 2004 年 2 月 9 日，该浏览器正式命名为”Mozilla Firefox”，并在 2004 年 11 月 9 日发布了 Firefox 的第一个正式版本 Firefox1.0，即火狐浏览器。</p>
<h2 id="IE-浏览器的衰败"><a href="#IE-浏览器的衰败" class="headerlink" title="IE 浏览器的衰败"></a>IE 浏览器的衰败</h2><p>Mozilla 组织在积极研发火狐浏览器的时候，微软这个时候在干啥呢？</p>
<p>微软继 IE3.0 之后又相继推出了后续版本，而 IE6.0 让 IE 浏览器到达了巅峰状态。IE6 成功后，微软认为它们的浏览器技术已经绝对领先了，于是…..解散了技术团队。</p>
<p><img src="https://ws1.sinaimg.cn/large/006tKfTcgy1fml0idx47tj308c07gwf9.jpg" alt=""></p>
<p>因此在 21 世纪初，Mozilla Firefox 重创 IE6.0，IE6.0 的地位变得岌岌可危。而 Firefox 在 2009 年迎来了顶峰，市占率接近 30%。</p>
<h2 id="谷歌浏览器的诞生"><a href="#谷歌浏览器的诞生" class="headerlink" title="谷歌浏览器的诞生"></a>谷歌浏览器的诞生</h2><p>在 Firefox 浏览器最辉煌的时候，迎来了让它逐步没落的对手 —— 2008 年 9 月 2 日，Google 发布了第一个 beta 版本的 Chrome 浏览器。</p>
<p>事实上，就在多年以前，Google 和 Mozilla 达成一项合作协议，双方在浏览器技术方面合作，同时 Mozilla 浏览器的搜索引擎默认为 Google，为此，Google 需每年向 Mozilla 支付1亿美元，但是没想到哇！养虎为患哇！¥#%&amp;@*… … Google 在 Mozilla 技术上融会贯通，构建了自己的 V8 引擎。</p>
<p><img src="https://ws3.sinaimg.cn/large/006tKfTcgy1fmkzv6kfvgj30b408cmx6.jpg" alt=""></p>
<p>从 Chrome 出现的那一天开始，消费者们就发现替代 IE 最好浏览器是 Chrome，因此 Firefox 也开始失宠了。</p>
<h2 id="第二次浏览器大战"><a href="#第二次浏览器大战" class="headerlink" title="第二次浏览器大战"></a>第二次浏览器大战</h2><p>虽然 Chrome 成为了当时最受欢迎的浏览器，但是非常要命的是，继网景将浏览器代码开源后，Chrome 也将浏览器代码开源了。也就是说大家都可以任意的裁剪编译源代码，由此衍生出了庞大的浏览器产业。苹果公司的 safari 浏览器，以小巧著名的 Opera 浏览器等等。一时间各大浏览器厂商都在想法设法的抢占市场占有率。</p>
<p>跟第一次浏览器大战相比，第二次浏览器大战的参赛人员明显更多，那会造成什么后果呢？</p>
<p>后果就是程序员们暴躁了。由于每一款浏览器的语法标准都不一样，也就意味着程序员们如果想要项目在每一款浏览器上都能正常运行，就必须针对不同的浏览器进行不同的开发。这样长久下去…程序员应该就会死绝了。</p>
<p>所以在这一次的浏览器大战中，浏览器厂商们为了不让程序员死绝，大家都逐渐意识到了“标准”的重要性。因为如果几款浏览器都采用统一标准，那么程序员针对这几款浏览器就只需要开发一个版本的项目就可以了。换句话说，也就是谁家的浏览器能更接近标准，谁也就能在市场上获得更高的认可。</p>
<p>所以第二次浏览器大战，成为了浏览器的标准统一之战。</p>
]]></content>
    
    <summary type="html">
    
      &lt;p&gt;作为一个前端开发人员，经常与浏览器打交道，了解浏览器的一些相关历史也非常有必要的。&lt;/p&gt;
    
    </summary>
    
    
      <category term="浏览器" scheme="http://yoursite.com/tags/%E6%B5%8F%E8%A7%88%E5%99%A8/"/>
    
      <category term="IE" scheme="http://yoursite.com/tags/IE/"/>
    
  </entry>
  
  <entry>
    <title>搭建 Github 个人博客(四)：配置博客评论功能</title>
    <link href="http://yoursite.com/2017/07/16/commentBlog/"/>
    <id>http://yoursite.com/2017/07/16/commentBlog/</id>
    <published>2017-07-16T07:49:19.000Z</published>
    <updated>2018-07-19T06:49:53.228Z</updated>
    
    <content type="html"><![CDATA[<p>之前写了三篇关于怎么在 Github Pages 上使用 Hexo 搭建个人博客的教程。这两天自己想着给博客加一个评论的功能，上网搜了一下教程，发现网上关于这方面的文章比较少。东拼西凑的看了好几篇文章，终于是把这个功能是弄出来了。</p>
<p>所以，弄好之后干脆就把自己的操作过程整理一下，写一篇关于给博客添加评论系统的教程。</p>
<a id="more"></a>
<h2 id="评论系统选择"><a href="#评论系统选择" class="headerlink" title="评论系统选择"></a>评论系统选择</h2><p>现在很多的技术博客都是程序员们自己搭建的，所以博客本身并不带有评论功能。这时候我们就需要借助第三方的评论系统，来为自己的博客添加评论功能。</p>
<p>上网搜了一下，现在使用人数比较多的几个第三方评论系统：</p>
<ul>
<li><strong>多说</strong>：以前是比较火的，但是在 2017 年 6 月 1 日已经正式关停服务，所以没法再使用了。</li>
<li><strong>畅言</strong>：搜狐旗下产品。但是需要个人博客备案后才能使用（我不会备案，所以放弃）。</li>
<li><strong>友言</strong>：JiaThis 旗下产品。但由于它是 Http 请求，而 Github Pages 现在是 Https 请求，在 Https 站点无法调用 Http 请求，所以也无法使用。</li>
<li><strong>网易云跟帖</strong>：我屁颠颠的去搜，结果告诉我，云跟帖产品将于2017年8月1日停止服务… …然后，没有然后了。</li>
<li><strong>Disqus</strong>：国外的评论系统。火是很火，但是国内被墙了，而且官网全英文。所以我放弃了。</li>
</ul>
<p>我能找到的几个主流的评论系统基本上已经全军覆没了。无意中看到有很多人在推荐 <strong>gitment</strong>，所以就去搜了一下。</p>
<blockquote>
<p>gitment 是国内的大神<a href="https://imsun.net/posts/gitment-introduction/" target="_blank" rel="external">孙士权</a>基于 GitHub 的 Issues 开发的一款评论系统。</p>
</blockquote>
<p>因此最终我选择了用 gitment 来搭建设置博客的评论功能（其实也没啥选择，只有这个能用）。</p>
<h2 id="申请一个Github-OAuth-Application"><a href="#申请一个Github-OAuth-Application" class="headerlink" title="申请一个Github OAuth Application"></a>申请一个Github OAuth Application</h2><p>使用 gitment 的第一步，就是需要去申请一个 GitHub 的 OAuth Application。</p>
<p>（1）点击个人头像选择 <strong>Settings</strong> 进入设置页面。</p>
<p>（2）选择下图左侧红框选中的选项 <strong>OAuth Apps</strong>。</p>
<p>（3）看到出现下图右侧中的提示，点击绿色按钮 <strong>Register a new application</strong> 注册一个应用程序。</p>
<p><img src="https://ws1.sinaimg.cn/large/006tKfTcgy1fixisaw6chj31kw19e0zu.jpg" alt=""></p>
<p>（4）按照下图填写相关信息。</p>
<p><img src="https://ws2.sinaimg.cn/large/006tKfTcgy1fixizjs8nfj31gk130tii.jpg" alt=""></p>
<p>（5）注册成功之后会跳转到一个新页面，在新页面中我们可以得到我们的 <strong>Client ID</strong> 和 <strong>Client Secret</strong>，如下图所示：</p>
<p><img src="https://ws2.sinaimg.cn/large/006tKfTcgy1fiw2mnzjhlj31ka0s0wja.jpg" alt=""></p>
<p style="color: gray">（我也不知道这两串数字能不能展示出来，所以先糊了再说。）</p>

<p>记住自己的 <strong>Client ID</strong> 和 <strong>Client Secret</strong>，后面需要用到。</p>
<h2 id="在博客中调用-gitment"><a href="#在博客中调用-gitment" class="headerlink" title="在博客中调用 gitment"></a>在博客中调用 gitment</h2><p>上一步中的 <strong>Client ID</strong> 和 <strong>Client Secret</strong> 得到之后，接下来我们需要在自己的博客页面（具体位置看后面解释）中添加如下代码：</p>
<figure class="highlight html"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div></pre></td><td class="code"><pre><div class="line"><span class="tag">&lt;<span class="name">div</span> <span class="attr">id</span>=<span class="string">"gitmentContainer"</span>&gt;</span><span class="tag">&lt;/<span class="name">div</span>&gt;</span></div><div class="line"><span class="tag">&lt;<span class="name">link</span> <span class="attr">rel</span>=<span class="string">"stylesheet"</span> <span class="attr">href</span>=<span class="string">"https://imsun.github.io/gitment/style/default.css"</span>&gt;</span></div><div class="line"><span class="tag">&lt;<span class="name">script</span> <span class="attr">src</span>=<span class="string">"https://imsun.github.io/gitment/dist/gitment.browser.js"</span>&gt;</span><span class="undefined"></span><span class="tag">&lt;/<span class="name">script</span>&gt;</span></div><div class="line"><span class="tag">&lt;<span class="name">script</span>&gt;</span><span class="javascript"></span></div><div class="line"><span class="keyword">var</span> gitment = <span class="keyword">new</span> Gitment(&#123;</div><div class="line">    <span class="attr">owner</span>: <span class="string">'Your GitHub username'</span>,</div><div class="line">    <span class="attr">repo</span>: <span class="string">'The repo to store comments'</span>,</div><div class="line">    <span class="attr">oauth</span>: &#123;</div><div class="line">        <span class="attr">client_id</span>: <span class="string">'Your client ID'</span>,</div><div class="line">        <span class="attr">client_secret</span>: <span class="string">'Your client secret'</span>,</div><div class="line">    &#125;,</div><div class="line">&#125;);</div><div class="line">gitment.render(<span class="string">'gitmentContainer'</span>);</div><div class="line"><span class="tag">&lt;/<span class="name">script</span>&gt;</span></div></pre></td></tr></table></figure>
<p><strong>说明</strong>：以上代码有四个需要修改的地方：</p>
<ol>
<li><code>Your GitHub username</code>：填写你的博客所在的 GitHub 账户名；</li>
<li><code>The repo to store comments</code>：填写存放评论的 GitHub 仓库名（由于 gitment 的评论是通过 issues 来存放的，所以建议这里填写个人博客所在的仓库）。</li>
<li><code>Your client ID</code>：前面所申请到的 Client ID。</li>
<li><code>Your client secret</code>：前面所申请到的 Client Secret。</li>
</ol>
<p>填完之后把代码保存然后执行以下命令上传到 Github 就可以了。</p>
<figure class="highlight bash"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">hexo g</div><div class="line">hexo d</div></pre></td></tr></table></figure>
<h3 id="调用-gitment-的位置"><a href="#调用-gitment-的位置" class="headerlink" title="调用 gitment 的位置"></a>调用 gitment 的位置</h3><p>上面的那一大段代码，到底添加到什么地方，是我纠结了半天的问题。搜索了很多都没有详细解释这一块内容的，原作者的博客中也只写了下面这一句话。</p>
<p><img src="https://ws3.sinaimg.cn/large/006tKfTcgy1fj2oa7gadhj314k04q74l.jpg" alt=""></p>
<p>我找了很久这个“你的页面”到底是指的哪个页面。</p>
<p>后来发现，实际上，这段代码的作用就是调用 gitment 并添加评论功能。所以，你博客里哪个页面需要评论功能，就在哪个页面添加这个代码。但是又会有一个新的问题，我们的每一篇博文都需要有评论功能，总不能每发一篇博文都去添加一次。</p>
<p>所以我去翻了一下我的主题文件目录。（我用的是<strong>Even</strong>主题，其他主题应该也差不多）。</p>
<p><img src="https://ws4.sinaimg.cn/large/006tKfTcgy1fj2u71luagj31kw0j3gvq.jpg" alt=""></p>
<p>我在主题文件里找到一个<code>comments.swig</code>文件，然后将上面的代码按照上图所示位置添加进去。再保存上传以后就成功了。</p>
<h2 id="初始化评论"><a href="#初始化评论" class="headerlink" title="初始化评论"></a>初始化评论</h2><p>由于 gitment 的原理是为每一遍博文以其 URL 作为标识创建一个 GitHub issue，对该篇博客的评论就是对这个 issue 的评论。因此，我们需要为每篇博文初始化一下评论系统，初始化后，你可以在你的 GitHub 上会创建相对应的 issue。</p>
<p>接下来，介绍一下如何初始化评论系统：</p>
<p>（1）上面的代码添加成功并上传后，你就可以在你的博文页下面看到一个评论框。还有一个报错提示，提示该篇博文的评论系统还没初始化。</p>
<p><img src="https://ws1.sinaimg.cn/large/006tKfTcgy1fixi2dvwwyj31ce0saq5m.jpg" alt=""></p>
<p>（2）点击登录之后，使用自己的 GitHub 账号登录后，就可以在上面错误信息处看到一个 <strong>Initialize Comments</strong> 的按钮。</p>
<p><img src="https://ws2.sinaimg.cn/large/006tKfTcgy1fixi3pioplj317o0qg406.jpg" alt=""></p>
<p>（3）点击 <strong>Initialize Comments</strong> 按钮后，就可以开始对该篇博文进行评论了，同时也可以在对应的 GitHub 仓库看到相应的 issue。</p>
<h2 id="安全问题"><a href="#安全问题" class="headerlink" title="安全问题"></a>安全问题</h2><p>因为我们在页面填写的 Client ID 和 Client Secret 别人都是可以看到的。虽然我也不知道这两串数字具体有什么用，但是如果这两串数字还有其他作用的话，那么明文显示在页面代码中就会存在安全性问题。但是也没有想到好的解决办法，所以暂时的办法就是创建一个小号，用小号的 Client ID 和 Client Secret。</p>
]]></content>
    
    <summary type="html">
    
      &lt;p&gt;之前写了三篇关于怎么在 Github Pages 上使用 Hexo 搭建个人博客的教程。这两天自己想着给博客加一个评论的功能，上网搜了一下教程，发现网上关于这方面的文章比较少。东拼西凑的看了好几篇文章，终于是把这个功能是弄出来了。&lt;/p&gt;
&lt;p&gt;所以，弄好之后干脆就把自己的操作过程整理一下，写一篇关于给博客添加评论系统的教程。&lt;/p&gt;
    
    </summary>
    
      <category term="博客" scheme="http://yoursite.com/categories/%E5%8D%9A%E5%AE%A2/"/>
    
    
      <category term="博客" scheme="http://yoursite.com/tags/%E5%8D%9A%E5%AE%A2/"/>
    
      <category term="Github" scheme="http://yoursite.com/tags/Github/"/>
    
      <category term="gitment" scheme="http://yoursite.com/tags/gitment/"/>
    
      <category term="评论" scheme="http://yoursite.com/tags/%E8%AF%84%E8%AE%BA/"/>
    
  </entry>
  
  <entry>
    <title>搭建 Github 个人博客(三)：设置 hexo 博客主题</title>
    <link href="http://yoursite.com/2017/07/15/themeBlog/"/>
    <id>http://yoursite.com/2017/07/15/themeBlog/</id>
    <published>2017-07-15T09:10:43.000Z</published>
    <updated>2018-08-16T01:54:23.637Z</updated>
    
    <content type="html"><![CDATA[<p>之前写了两篇文章来教大家怎么去创建 GitHub 个人博客，然后再怎么配合 hexo 去开发博客。<br><a id="more"></a></p>
<ul>
<li><a href="https://runjaelyn.github.io/2017/06/09/signupBlog/" target="_blank" rel="external">搭建 Github 个人博客(一)：搭建博客基础页面</a> </li>
<li><a href="https://runjaelyn.github.io/2017/06/25/hexoBlog/" target="_blank" rel="external">搭建 Github 个人博客(二)：配合使用 hexo 开发博客</a>  </li>
</ul>
<p>本来这个教程到此也差不多结束了。但是后来有些笨鸡娃子找到我说，不会设置博客主题，所以还是决定再写最后一篇教程讲一下怎么去用 hexo 去设置博客的主题。</p>
<h2 id="选择主题"><a href="#选择主题" class="headerlink" title="选择主题"></a>选择主题</h2><p>在设置主题之前，首先需要去找一款喜欢的主题样式。hexo 有自己的博客主题官网 <a href="https://hexo.io/themes/" target="_blank" rel="external">Themes | Hexo</a>，可以在官网里去找喜欢的主题样式。百度上也可以搜到一些开发者们自己写的一些好看主题。具体的自行百度，随便一搜就出来很多了。</p>
<p><img src="https://ws4.sinaimg.cn/large/006tKfTcgy1fi147brqq9j30w20fan17.jpg" alt=""></p>
<p>选择好主题后，找到该主题的地址。我们用官网的一个主题为例：</p>
<p>（1）点击主题名字进入主题详情页面。</p>
<p><img src="https://ws1.sinaimg.cn/large/006tKfTcgy1fhzlcff7x5j31kw0th47f.jpg" alt=""></p>
<ul>
<li><p>详情页面里有一个<code>README.md</code>的主题说明的文档。大部分的主题在这个文档里都会有下载安装配置的流程说明。</p>
<p>  <img src="https://ws1.sinaimg.cn/large/006tKfTcgy1fhzlgmy5d8j31ia0voq74.jpg" alt=""></p>
<p>  例如我们当前选择的这个主题，详情页面往下拉就能看到下载安装主题需要执行的相关命令，直接按照文档里给出的命令依次去执行就可以了。</p>
<p>  如果遇到文档没有写安装教程的，或者 … 看不懂英文的 … 那就继续看我们这个教程的下一步吧。</p>
</li>
</ul>
<p>（2）点击详情页面的绿色按钮，复制红框中的主题地址。</p>
<p><img src="https://ws2.sinaimg.cn/large/006tKfTcgy1fhzjm91vxmj31kw0p77ac.jpg" alt=""></p>
<p>在其他网站找的主题也一样的会有一个主题的地址，同样的复制下来就行。</p>
<h2 id="主题安装"><a href="#主题安装" class="headerlink" title="主题安装"></a>主题安装</h2><p>有了主题地址之后，接下来我们就可以开始安装了。</p>
<p>（1）打开命令行工具，进入到你的博客文件夹。然后执行以下格式的命令，将选中的主题克隆下来。</p>
<figure class="highlight js"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">git clone <span class="string">"url"</span> themes/<span class="string">"themeName"</span>    <span class="comment">// 不要复制这一行！！！看下面解释！！！</span></div></pre></td></tr></table></figure>
<ul>
<li><code>&quot;url&quot;</code>就是我们刚刚复制的主题地址。</li>
<li><code>&quot;themeName&quot;</code>是我们当前选择的主题名字</li>
</ul>
<p>将你找到的主题地址和主题名字替换掉上面的<code>&quot;url&quot;</code>和<code>&quot;themeName&quot;</code>。</p>
<p>例如我们刚刚在官网找到的那个主题，执行命令如下：</p>
<figure class="highlight bash"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">git <span class="built_in">clone</span> https://github.com/levblanc/hexo-theme-aero-dual.git themes/aero-dual</div></pre></td></tr></table></figure>
<ul>
<li><code>themes/aero-dual</code>前面的<code>themes</code>是存储所有主题文件的文件夹名字，<code>aero-dual</code>是当前主题文件夹的名字，也是当前主题的名字。</li>
</ul>
<p>（2）看到出现类似下面这种 100% 的命令，就表示主题下载成功了。</p>
<p><img src="https://ws1.sinaimg.cn/large/006tKfTcgy1fhzjxhuws9j311m05o412.jpg" alt=""></p>
<p>（3）下载成功之后，在我们的博客文件目录就可以看到我们刚刚下载好的主题文件。</p>
<p><img src="https://ws4.sinaimg.cn/large/006tKfTcgy1fi14mvl2jjj30wg06mq3x.jpg" alt=""></p>
<p>（4）用编辑器打开博客文件目录中的<code>_config.yml</code>文件。将主题配置改为当前选择的主题名字。</p>
<figure class="highlight yaml"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line"><span class="comment"># Extensions</span></div><div class="line"><span class="comment">## Plugins: https://hexo.io/plugins/</span></div><div class="line"><span class="comment">## Themes: https://hexo.io/themes/</span></div><div class="line"><span class="attr">theme:</span> <span class="string">aero-dual</span></div></pre></td></tr></table></figure>
<p>（5）通过命令进入到当前主题文件夹，例如：</p>
<figure class="highlight bash"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line"><span class="built_in">cd</span> themes/aero-dual</div></pre></td></tr></table></figure>
<p><code>aero-dual</code>这是我的主题名字！你们自己换成你们自己选择的主题名字。</p>
<p>（6）然后再执行下面的代码，将当前更改的信息进行合并。</p>
<figure class="highlight bash"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">git pull</div></pre></td></tr></table></figure>
<p>（7）最后，主题配置完成。回到博客文件目录执行以下命令重新部署整个博客。</p>
<figure class="highlight bash"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">hexo g</div><div class="line">hexo d</div></pre></td></tr></table></figure>
<p>好，完成。重新打开博客就可以发现新的主题已经生效了。</p>
<p><img src="https://ws1.sinaimg.cn/large/006tKfTcgy1fhzqf6q16dj31kw10bgzu.jpg" alt=""></p>
<h2 id="功能配置"><a href="#功能配置" class="headerlink" title="功能配置"></a>功能配置</h2><p>到上一步，我们的主题其实就已经安装好了。博客基本上也可以正常使用了。</p>
<p>但是有的时候，博客或者主题的一些默认配置并不是我们想要的，所以我们还可以根据自己的需求去更改一些配置。这里简单梳理几个常用的配置信息。</p>
<h3 id="博客配置"><a href="#博客配置" class="headerlink" title="博客配置"></a>博客配置</h3><p>找到博客目录下的<code>_config.yml</code>文件。</p>
<p>注意：以下所有配置，冒号后面都要有一个空格。</p>
<figure class="highlight yaml"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line"><span class="attr">title:</span>                       <span class="comment">## 博客主标题 </span></div><div class="line"><span class="attr">subtitle:</span>                    <span class="comment">## 博客副标题  </span></div><div class="line"><span class="attr">language:</span> <span class="string">zh-CN</span>              <span class="comment">## 语言 中文</span></div></pre></td></tr></table></figure>
<h3 id="主题配置"><a href="#主题配置" class="headerlink" title="主题配置"></a>主题配置</h3><p>每一个主题的文件目录下也都有一个<code>_config.yml</code>文件，这个就是主题配置文件。不同的主题配置信息也不一样。</p>
<h4 id="评论功能"><a href="#评论功能" class="headerlink" title="评论功能"></a>评论功能</h4><p>因为添加评论功能的配置比较复杂，所以单独写了一篇博客来讲解。</p>
<p><a href="https://runjaelyn.github.io/2017/07/16/commentBlog/" target="_blank" rel="external">《搭建 Github 个人博客(四)：配置博客评论功能》</a></p>
<h3 id="其他配置"><a href="#其他配置" class="headerlink" title="其他配置"></a>其他配置</h3><p><strong>文章摘要</strong></p>
<p>有一些主题博客首页默认显示全文，如果只希望现实某一个段落在首页，可以在文章内容中插入<code>&lt;!--more--&gt;</code>以隐藏后面的内容。</p>
<hr>
<p>所有关于博客搭建的教程就写到这了。以后想到还有什么漏掉的再来修改。</p>
]]></content>
    
    <summary type="html">
    
      &lt;p&gt;之前写了两篇文章来教大家怎么去创建 GitHub 个人博客，然后再怎么配合 hexo 去开发博客。&lt;br&gt;
    
    </summary>
    
      <category term="博客" scheme="http://yoursite.com/categories/%E5%8D%9A%E5%AE%A2/"/>
    
    
      <category term="博客" scheme="http://yoursite.com/tags/%E5%8D%9A%E5%AE%A2/"/>
    
      <category term="theme" scheme="http://yoursite.com/tags/theme/"/>
    
  </entry>
  
  <entry>
    <title>搭建 Github 个人博客(二)：配合使用 hexo 开发博客</title>
    <link href="http://yoursite.com/2017/06/25/hexoBlog/"/>
    <id>http://yoursite.com/2017/06/25/hexoBlog/</id>
    <published>2017-06-25T06:31:42.000Z</published>
    <updated>2018-07-19T06:46:05.070Z</updated>
    
    <content type="html"><![CDATA[<p>在<a href="https://runjaelyn.github.io/2017/06/09/signupBlog/" target="_blank" rel="external">《搭建 Github 个人博客(一)：搭建博客基础页面》</a>中我们讲了怎么去搭建一个 GitHub 博客的基础页面。实际上，基础页面搭建完成之后，我们就已经可以通过 Github Pages 直接写 HTML 静态博客了。</p>
<a id="more"></a>
<p>但是尝试之后我们会发现，这种博客使用起来相当麻烦。页面不够好看，操作又不方便，后期维护起来又很繁杂。所以，我们需要借用一些静态博客框架来辅助我们开发 github 博客。</p>
<p>现在比较流行的静态博客框架也比较多，不同的静态程序可以说都有各自的好处。这篇文章我们主要讲的是使用<a href="https://hexo.io/" target="_blank" rel="external">HEXO</a>这个框架来搭建 GitHub 个人博客。</p>
<h2 id="运行环境准备"><a href="#运行环境准备" class="headerlink" title="运行环境准备"></a>运行环境准备</h2><p>hexo 是一款基于 Node.js 的静态博客框架，所以我们需要安装 Node.js。</p>
<ul>
<li><a href="https://nodejs.org/en/download/" target="_blank" rel="external">Node.js</a>（下载最新版的Node.js）</li>
<li><a href="https://git-scm.com/" target="_blank" rel="external">Git</a></li>
<li>文本编辑器（用于修改配置文件以及写文章）</li>
</ul>
<h2 id="安装hexo"><a href="#安装hexo" class="headerlink" title="安装hexo"></a>安装hexo</h2><p>当 Node.js 和 Git 都安装好了之后，就可以正式安装 hexo 了。</p>
<p>（1）打开命令行工具（Mac：终端 <code>Terminal</code> / windows：CMD） ，执行以下命令：</p>
<figure class="highlight bash"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">sudo npm install -g hexo</div></pre></td></tr></table></figure>
<p>注意：Hexo 官网上的安装命令是 <code>npm install -g hexo-cli</code>，安装时不要忘记前面加上<code>sudo</code>，否则会因为权限问题报错。</p>
<ul>
<li>选装 cnpm</li>
</ul>
<p>因为 npm 安装插件是从国外服务器下载，受网络影响大，可能出现异常。所以我们可以利用淘宝提供的国内镜像。</p>
<p>命令行执行以下代码：</p>
<figure class="highlight bash"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">npm --registry=https://registry.npm.taobao.org install -g cnpm</div></pre></td></tr></table></figure>
<p>安装完后最好通过 <code>cnpm -v</code> 命令查看其版本号，或关闭终端重新打开。安装完直接使用有可能会出现错误。</p>
<p><code>cnpm</code> 跟 <code>npm</code> 用法完全一致，只是在执行命令时将 <code>npm</code> 改为 <code>cnpm</code>（如果使用了淘宝镜像，后面操作将以 <code>cnpm</code> 代替 <code>npm</code>）</p>
<p>（2）输入管理员密码即开始安装 </p>
<p>（3）新建一个文件夹，例如 <code>blog</code>。用来存放自己博客相关的文件。</p>
<p>（4）在终端里通过 <code>cd</code> 命令进入到 <code>blog</code> 文件夹。</p>
<p><img src="https://ws1.sinaimg.cn/large/006tNc79gy1fgxfdois13j30r804gwf5.jpg" alt=""></p>
<p>（5）确定进入到 <code>blog</code> 文件夹之后，执行以下命令，对 <code>blog</code> 文件夹进行初始化。</p>
<figure class="highlight bash"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">hexo init</div></pre></td></tr></table></figure>
<p>（6）初始化完成之后，执行以下命令，安装所有的依赖和插件。</p>
<figure class="highlight bash"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">npm install</div></pre></td></tr></table></figure>
<p>（7）安装完成后，执行以下命令，开启 hexo 本地服务器。</p>
<figure class="highlight bash"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">hexo sever		或简写 hexo s</div></pre></td></tr></table></figure>
<p>当看到以下命令出现，就表示服务器启动成功。</p>
<p><img src="https://ws1.sinaimg.cn/large/006tNc79gy1fgxfek2yetj30ru0143yr.jpg" alt=""></p>
<p>然后就可以在浏览器中打开<a href="http://localhost:4000" target="_blank" rel="external">http://localhost:4000</a>来查看博客了。</p>
<p><img src="https://ws4.sinaimg.cn/large/006tNc79gy1fgxff2rb0oj30qk0d1n3y.jpg" alt=""></p>
<p>至此，我们的 hexo 静态博客框架安装就完成了。</p>
<h2 id="关联-github"><a href="#关联-github" class="headerlink" title="关联 github"></a>关联 github</h2><p>现阶段，我们只是在本地搭建好了博客页面，其他人是无法访问的。所以，本地安装完成之后，接下来我们需要关联 github。</p>
<p>（1）用编辑器打开 <code>blog</code> 文件夹里面的一个 <code>_config.yml</code> 的文件。将文件末尾修改为以下内容：</p>
<p>注：<code>runjaelyn</code> 改为你自己 <code>github</code> 的用户名</p>
<figure class="highlight yaml"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line"><span class="attr">deploy:</span> </div><div class="line"><span class="attr">  type:</span> <span class="string">git</span></div><div class="line"><span class="attr">  repo:</span> <span class="attr">https://github.com/runjaelyn/runjaelyn.github.io.git</span></div><div class="line"><span class="attr">  branch:</span> <span class="string">master</span></div></pre></td></tr></table></figure>
<p style="color:red">注意：在配置所有的 _config.yml 文件时（包括后面会用到的主题 theme 文件夹中的），所有的冒号 : 后边都要加一个空格，否则执行 hexo 命令会报错。</p>

<p>（2）修改完成并保存。然后通过 <code>cd</code> 命令进入到 <code>blog</code> 文件夹目录，在命令行执行生成静态页面的命令：</p>
<figure class="highlight bash"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">hexo generate		或简写 hexo g</div></pre></td></tr></table></figure>
<ul>
<li><p>此时若出现如下报错：</p>
  <figure class="highlight bash"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">ERROR Local hexo not found <span class="keyword">in</span> ~/blog</div><div class="line">ERROR Try runing: <span class="string">'npm install hexo --save'</span></div></pre></td></tr></table></figure>
</li>
</ul>
<ul>
<li><p>则执行命令：</p>
  <figure class="highlight bash"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">npm install hexo --save</div></pre></td></tr></table></figure>
</li>
</ul>
<ul>
<li>若无报错，自行忽略此步骤。</li>
</ul>
<p>（3）静态页面生成成功之后，执行配置命令 <code>deploy</code>，部署网站。（以后每次部署网站前，都需要预先生成静态文件）</p>
<figure class="highlight bash"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">hexo deploy		或简写 hexo d</div></pre></td></tr></table></figure>
<p>（4）部署网站执行成功后，会显示类似于下列的命令。</p>
<p><img src="https://ws3.sinaimg.cn/large/006tNc79gy1fgxffveqejj31100ae782.jpg" alt=""></p>
<p>但是，也有可能会出现几种报错的情况：</p>
<p style="color:red">1. 无法连接 Git 或找不到 Git。</p>

<p>则执行如下命令来安装 <code>hexo-deployer-git</code>。</p>
<figure class="highlight bash"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">npm install hexo-deployer-git --save</div></pre></td></tr></table></figure>
<p><img src="https://ws3.sinaimg.cn/large/006tNc79gy1fgxfg618n4j30qw05ktae.jpg" alt=""></p>
<p style="color:red">2. 没有配置用户名和密码。</p>

<p>若在本机上是第一次使用 Git，那么 Git 会要求配置的是你个人的用户名称和电子邮件地址。</p>
<p><img src="https://ws2.sinaimg.cn/large/006tNc79gy1fgxfgf2dzij30ow05qq3n.jpg" alt=""></p>
<p>这两条配置很重要，每次 Git 提交时都会引用这两条信息，用来说明是谁提交了更新。所以，需要则执行以下命令：</p>
<figure class="highlight bash"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">git config --global user.name <span class="string">"name"</span></div><div class="line">git config --global user.email <span class="string">"name@example.com"</span></div></pre></td></tr></table></figure>
<p>上面<code>&quot;&quot;</code>里的内容填写自己的名字和邮箱就可以了。</p>
<p style="color:red">3. 未关联 GitHub。</p>

<p>若你未关联 Github，那么在执行 <code>hexo deploy</code> 命令时终端会提示你输入Github 的用户名和密码，即</p>
<p><img src="https://ws4.sinaimg.cn/large/006tNc79gy1fgxfh04943j31140baae1.jpg" alt=""></p>
<p>依次输入你自己的 github 账号和密码就可以了。</p>
<p>（5）以上报错问题解决好了之后，重新执行配置命令 <code>deploy</code>，部署网站。</p>
<figure class="highlight bash"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">hexo deploy		或简写 hexo d</div></pre></td></tr></table></figure>
<p>（6）显示成功之后，打开浏览器，输入你自己的 GitHub 地址。</p>
<p><img src="https://ws3.sinaimg.cn/large/006tNc79gy1fgxfhczaskj30mc01qjri.jpg" alt=""></p>
<p><code>runjaelyn</code> 改为你自己的用户名。</p>
<p>至此，我们刚刚在本地搭建的博客页面，现在就已经关联到了 github 上，其他人可以通过你的 Github 博客地址访问你的博客了。</p>
<h2 id="发表新文章"><a href="#发表新文章" class="headerlink" title="发表新文章"></a>发表新文章</h2><p>博客搭建好了之后，我们就可以开始往博客里添加我们的博客内容了。</p>
<p>（1）通过 <code>cd</code> 进入到 <code>blog</code> 文件夹，执行以下命令：</p>
<figure class="highlight bash"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">hexo new <span class="string">"文章标题"</span></div></pre></td></tr></table></figure>
<p>创建成功后会有如下提示：</p>
<p><img src="https://ws1.sinaimg.cn/large/006tNc79gy1fgxfice2lyj30ms01s0tb.jpg" alt=""></p>
<p>创建成功后，打开 <code>blog</code> 文件夹里的 <code>source／_posts</code> ，就可以看到我们刚刚通过 hexo <code>new</code> 出来的文章 <code>.md</code> 文件。    </p>
<p>然后我们就可以添加我们的文章内容了（内容遵循 <code>Markdown</code> 的语法规则）。 </p>
<p>（2）文章编辑好保存后，执行如下命令，重新生成并部署：</p>
<figure class="highlight bash"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">hexo clean </div><div class="line">hexo g </div><div class="line">hexo d</div></pre></td></tr></table></figure>
<p>（3）成功后刷新博客就可以看到新文章已经显示在页面了。</p>
<p>到此为止，我们的博客就搭建完成了。</p>
<p>如果觉得 hexo 默认主题样式不好看的话，还可以自己去网上找其他主题，更换主题模式。</p>
<p>以后，我们就都可以通过 hexo 来管理操作我们的博客了。</p>
]]></content>
    
    <summary type="html">
    
      &lt;p&gt;在&lt;a href=&quot;https://runjaelyn.github.io/2017/06/09/signupBlog/&quot; target=&quot;_blank&quot; rel=&quot;external&quot;&gt;《搭建 Github 个人博客(一)：搭建博客基础页面》&lt;/a&gt;中我们讲了怎么去搭建一个 GitHub 博客的基础页面。实际上，基础页面搭建完成之后，我们就已经可以通过 Github Pages 直接写 HTML 静态博客了。&lt;/p&gt;
    
    </summary>
    
      <category term="博客" scheme="http://yoursite.com/categories/%E5%8D%9A%E5%AE%A2/"/>
    
    
      <category term="博客" scheme="http://yoursite.com/tags/%E5%8D%9A%E5%AE%A2/"/>
    
      <category term="hexo" scheme="http://yoursite.com/tags/hexo/"/>
    
  </entry>
  
  <entry>
    <title>搭建 Github 个人博客(一)：搭建博客基础页面</title>
    <link href="http://yoursite.com/2017/06/09/signupBlog/"/>
    <id>http://yoursite.com/2017/06/09/signupBlog/</id>
    <published>2017-06-09T15:25:01.000Z</published>
    <updated>2018-07-19T06:46:15.941Z</updated>
    
    <content type="html"><![CDATA[<p>现在网络上技术类相关的博客由很多，很多类似于CSDN、博客园也都可以写文章。我们这篇教程主要介绍的是用 githubpages 搭建自己独立的技术博客。</p>
<a id="more"></a>
<h2 id="注册账号"><a href="#注册账号" class="headerlink" title="注册账号"></a>注册账号</h2><p>要创建 GitHub 的个人技术博客，首先需要进入<a href="https://github.com/" target="_blank" rel="external">Github官网</a>注册一个 GitHub 的账号。</p>
<p>（如果已经有 <code>github</code> 账号，可跳前两个步骤，直接进入第三步。）</p>
<p><img src="https://ws2.sinaimg.cn/large/006tNbRwgy1fgfcyl3w5wj31kw0yoaiu.jpg" alt=""></p>
<h2 id="初始设置"><a href="#初始设置" class="headerlink" title="初始设置"></a>初始设置</h2><p>账号注册成功之后会跳到一个账户初始设置的页面，一般情况下我们采用默认设置就行。不用做任何更改，直接点击绿色 <code>Continue</code> 按钮。</p>
<p><img src="https://ws2.sinaimg.cn/large/006tNbRwgy1fgfcykhxlbj31kw0xggtm.jpg" alt=""></p>
<h3 id="验证邮箱"><a href="#验证邮箱" class="headerlink" title="验证邮箱"></a>验证邮箱</h3><p>初始设置完成之后，打开你注册账号时填写的邮箱，查看 GitHub 发给你的邮件。</p>
<p>我们需要对邮箱进行验证，后面生成的个人主页才会被接受和发布。</p>
<p><img src="https://ws2.sinaimg.cn/large/006tNbRwgy1fgfcylv8fkj31kw0dy42y.jpg" alt=""></p>
<p>点击验证邮箱，验证成功后跳转到如下页面。</p>
<p><img src="https://ws3.sinaimg.cn/large/006tNbRwgy1fgfc10zrt7j31kw0tcjx9.jpg" alt=""></p>
<p>点击第二个按钮，创建一个新项目。</p>
<h2 id="创建页面仓库"><a href="#创建页面仓库" class="headerlink" title="创建页面仓库"></a>创建页面仓库</h2><p>这个页面用来填写关于你新建的这个仓库的一些初始化设置。</p>
<p>（找不到页面入口的可以点击这里<a href="https://github.com/new" target="_blank" rel="external">创建页面仓库</a>）</p>
<p>注意：</p>
<ol>
<li><p>仓库名称尽量跟你的 GitHub 用户名（注册账号时填写的用户名）保持一致。</p>
</li>
<li><p>因为 Github Pages 发布的页面统一使用 <code>github.io</code> 域名，所以你的仓库名还需要在你的用户名后面加上后缀 <code>.github.io</code>。</p>
</li>
</ol>
<p><img src="https://ws3.sinaimg.cn/large/006tNbRwgy1fgfcymfif2j31kw0ypgu4.jpg" alt=""></p>
<p>其他的一些可选选项根据自己需求填写，完成之后点击绿色按钮 <code>Create reponsitory</code> 创建仓库。</p>
<h2 id="生成页面"><a href="#生成页面" class="headerlink" title="生成页面"></a>生成页面</h2><p>仓库创建好后，接下来就是在仓库中生成我们的页面。</p>
<p><img src="https://ws2.sinaimg.cn/large/006tNbRwgy1fgfcyj5d3uj31kw0yrjyl.jpg" alt=""></p>
<p>点击页面内的 <code>setting</code> ，进入设置页面。</p>
<p>在生成完整的页面之前，我们需要先给我们的页面设置一个主题样式，所以滑到页面后面，可以看到有一个主题选择按钮，点击 <code>choose a theme</code>。</p>
<p><img src="https://ws4.sinaimg.cn/large/006tNbRwgy1fgfcyjvaofj317s0yi44h.jpg" alt=""></p>
<p>在主题页面选择一个自己喜欢的主题，选定后点击 <code>select theme</code></p>
<p><img src="https://ws2.sinaimg.cn/large/006tNbRwgy1fgfcymovenj31kw10gwok.jpg" alt=""></p>
<p>主题设置成功后会跳转到一个“README“文件的配置页面。</p>
<p><img src="https://ws1.sinaimg.cn/large/006tNbRwgy1fgfcykpcpoj31kw0pjaeo.jpg" alt=""></p>
<p>因为我们是将“README”作为我们博客的首页，所以选择直接将它提交到主分支即可。</p>
<p>其他的设置在后面我们都可以修改，所以这里不做任何改动，采用默认设置。</p>
<p>然后点击绿色 <code>Commit changes</code> 按钮。</p>
<p>到这为止，我们的 GitHub 博客就已经搭建好了。</p>
<h2 id="博客地址"><a href="#博客地址" class="headerlink" title="博客地址"></a>博客地址</h2><p>那么，别人要怎么访问我们的博客呢？</p>
<p>还是进入到我们刚刚选择主题时的 <code>setting</code> 页面。</p>
<p><img src="https://ws1.sinaimg.cn/large/006tNbRwgy1fgfcyiik04j315u0zmq8v.jpg" alt=""></p>
<p>在主题选择这一栏的上方，有一个链接。点击这个链接，就可以看到我们的博客首页了，里面有我们刚刚设置的博客主题，和“REANME”文件里的内容。</p>
<p><img src="https://ws4.sinaimg.cn/large/006tNbRwgy1fgfcympialj31kw0xigs1.jpg" alt=""></p>
<p>这个链接地址就是我们的博客地址，别人也就可以通过这个地址来访问我们 GitHub 博客了。</p>
]]></content>
    
    <summary type="html">
    
      &lt;p&gt;现在网络上技术类相关的博客由很多，很多类似于CSDN、博客园也都可以写文章。我们这篇教程主要介绍的是用 githubpages 搭建自己独立的技术博客。&lt;/p&gt;
    
    </summary>
    
      <category term="博客" scheme="http://yoursite.com/categories/%E5%8D%9A%E5%AE%A2/"/>
    
    
      <category term="博客" scheme="http://yoursite.com/tags/%E5%8D%9A%E5%AE%A2/"/>
    
      <category term="Github" scheme="http://yoursite.com/tags/Github/"/>
    
  </entry>
  
</feed>
